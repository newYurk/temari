# На чём стоят наши числа

Один список: какие величины в проекте взяты **из источника**, какие — **наш выбор**, и что сломается, если выбор неверен. Здесь ничего не объясняется заново — только где число живёт и на чём держится; сами постановки в [spec/](../spec), контракты в [rules.md](../src/components/temari/rules.md).

Заведено 17.09.2026. Правило простое: **если числа нет здесь и нет в источнике — его нет.**

## 1. Числа, которые держатся на нашем выборе

| Величина | Где стоит | На чём держится | Что от неё зависит |
|---|---|---|---|
| разметка `mark = 0,2 мм`; полная ширина студийной ленты | `measure.ts`, `thread.ts`, `stitches.ts`; [ремесленные основания](../spec/craft-sources.md#разметочная-и-рабочая-нить-уточнение-23092026) | **назначенный размер**, не измерение DMC Art 282 или lamé. В `862a625` множители `0,55 × 1,08` сужали ленту до ≈0,1188 мм; исправление передаёт в меш полные 0,2 мм без них | соотношение видимых ширин; это исправление контракта меша, не калибровка пары материалов и не доказательство универсального отношения к perle №5/8 |
| нулевая объёмная толщина ленты; положение на `R + 0,1 мм` при `mark = 0,2 мм`, без дополнительного lift | `stitches.ts` `ribbonFromPoints` / `vec` | **студийная поверхность и назначенное смещение**, не объёмное сечение металлической нити | перед контактами нужно определить материал и сечение дзивари вместе с рабочей нитью; видимая ширина ленты не задаёт радиус контактного препятствия |
| прямой канал шириной 2 / 11 мм; C240, слой 1,2 мм, радиусы нити / иглы / дзивари 0,355 / 0,2 / 0,1 мм | `needle-channel.ts`, `single-needle-catch.ts`; [контракт](../spec/needle-channel.md) | **инженерные условия**: фиксированная ось, непроницаемое ядро и разрешённый наружный слой, неподвижная разметка. Слой не является рассчитанной податливостью или пустым отверстием; нить толще иглы, расширение отверстия не решено | узкий контроль сохраняет коллизию жёстких объёмов, не доказывая невозможность реальной вышивки. Широкий положительный геометрический тест не исправляет GT14; выступание объёма над номинальной сферой остаётся видимым |
| заданные наружные кривые; ручки Безье у концов канала 6 мм | `single-needle-catch.ts` | **предписанная удерживаемая геометрия**, не результат натяжения. S8 даёт лишь положение; центральная нить оставлена на оси иглы как дополнительное условие | непрерывность и изгиб всего пути; прохождение геометрических проверок не подтверждает форму после затягивания |
| материал 120 мм, независимая подача 60 мм, остаток подачи — прямой хвост | `single-needle-catch.ts`, `thread-material-ledger.ts`; [баланс](../spec/needle-channel.md#реализуемый-единичный-контроль) | **назначенный нерастяжимый контроль**: положение свободного конца зависит от остатка длины; обмен не подгоняется к построенной кривой. Остаток запаса 60 мм без пространственной реконструкции | недостаточная подача или отрицательный запас дают отказ ledger. `conserved` — скалярный баланс, не трение, закрепление, равновесие или проверенный расход изделия |
| три визита, окно измерения ±3 мм; круглая проба радиусом 0,355 мм | `upper-studio-baseline.ts` | **выбор диагностического наблюдения**, не закон материала. Полилинии — фактическая итоговая геометрия студии, расстояния — до конечных отрезков; круг шириной 0,71 мм не равен сплющенному сечению | область замера лесенки и поиск подозрительных сближений; отрицательный зазор круглой пробы не доказывает пересечение настоящих объёмов |
| C240, круг Ø0,71 мм; заданные проходы и исходные кривые трёх визитов | `upper-bundle.ts`, параметры из `UPPER_KIKU_INPUT` | **инженерный начальный путь**, не численно принятая укладка и не точная копия студии. Концы проходов выше поверхности; обмен материалом на срезах не задан; длина кривых не назначена длиной покоя | новый контроль `?upper-bundle=1`; совместная укладка не решается, геометрическая проверка отклоняет исходный путь |
| запрос сетки `ceil(L/ρ)` и факторы 1,5 / 2 / 2,5, бюджет 256 точек | `upper-bundle.ts` | **правило дискретизации и вычислительный бюджет**, не физический минимум числа точек. Последний уровень третьего визита запрашивает 276/278 | будущий расчёт требует обоснованного бюджета/дискретизации; текущий отчёт сохраняет все уровни без усечения |
| `ρ_min = 0,25 мм` — предел изгиба нити | `lower-kagari.ts`, `s8-kiku.ts` | **наше инженерное допущение**, не измерено; даёт `r/ρ_min = 0,8` | форма нити у выхода из прокола и вся принятая лестница сеток |
| `r = 0,2 мм` — рабочий радиус расчётной нити | там же | **наш выбор** для контрольного образца; гейдж перле №5 даёт шаг соседних нитей ~0,71 мм, не измеренный радиус круглого сечения 0,355 мм | все зазоры, контакты и принятые этапы S8 |
| глубина прокола `1 мм`, полуширина подхвата `0,6 мм` | `lower-kagari.ts` | **заданы условиями**, не восстановлены по фотографии | геометрия нижнего подхвата |
| нижний шаг ряда | `s8-kiku.ts` `lowerRowAdvanceMm ≈ 3,4 мм` | **принцип — craft, величина — геометрическое допущение**: нижний стежок удерживает уложенную нить там, где она пересекает разметку ([Stretch the Points](https://www.temarikai.com/HowToPages/stretchpoints.html)). `2r/sin(α/2)` — сдвиг вершины идеального острого V при параллельном смещении прямых сторон; для конечных скруглённых нитей и последующих рядов требует проверки. Прежний зазор под GC-порты отклонён 22.09 | второй+ ряды uwagake; число не подтверждено ремесленной приёмкой |
| сжатия и перемещения прежнего пучка нет | модель второго ряда | **идеализация**: ранний материал фиксирован; не моделирует стягивание верхним стежком и приглаживание, описанное GT14 | плотность второго ряда и форма клина |
| круглое несжимаемое сечение | [spec/taut-contact.md](../spec/taut-contact.md) §1 | **идеализация**, не наблюдение за нитью | вид перехлёстов и контакты; 0,71 × 0,46 мм вне пересечения и 0,37 мм в пересечении — условные оценки по другим материалам ([нить на нить](thread-over-thread.md)), не измерения перле. В рендере 0,71 × 0,355 (`STITCH_FLAT` 0,5), [#93](https://github.com/newYurk/temari/issues/93) |
| подъём старого пути `STACK_LIFT = 0,42` ширины при `STITCH_FLAT = 0,5` | `stitches.ts`, `?pile=0` | **студийные коэффициенты**, не следствие круглого сечения: 0,42·0,71 ≈ 0,30 мм меньше назначенной высоты 0,355 мм. В модели двух равных круглых несжимаемых нитей контакт осей на диаметре верен, но это другая модель | вид перехлёстов; назначенные сечения старого пути могут пересекаться |
| ширина бугорка `0,045` длины стежка | `kagari.ts` `stackBump` | **ничем не обосновано**: доля стежка, а не длина | форма подъёма — [#94](https://github.com/newYurk/temari/issues/94) |
| шаг скрутки `3,6` толщины | `stitches.ts` `PERLE_TWIST_PITCH` | **оценка**: фото 3,4–3,9 и расчёт 4–6; крутку перле никто не публикует | только вид нити — [#16](https://github.com/newYurk/temari/issues/16) |
| подхват `0,71 мм` поперёк метки | `kagari.ts` `cornerMm` | **наш выбор**: равен принятому шагу уложенной перле №5 | ширина подхвата у метки |
| окно клипа обратного подхвата `6 мм` | `stitches.ts` `REVERSE_JOIN_MM` | **студийная отрисовка**: обрезка фланка снаружи всего catch; для stacked — не меньше `portExtent+pearl` | где кончается surface и начинается видимый нырок |
| окно подъёма у острия `4 мм` | `stitches.ts` `REVERSE_HUG_MM` | **студийная отрисовка**: гауссов `lift0=pearl·1,4` на исходящей | исходящая поверх входящей у tip |
| дно студийного scoop / глубина прокола | `kagari.ts` `scoopRadius`; `stitches.ts` `NEEDLE_DEPTH_MM=1` | **студийная отрисовка**: floor `1−half−0,3 мм`; прокол не глубже 1 мм под единичной сферой; pierce-band ≤ 0,85·tip→port | bury start/stop и короткий нырок у порта; не измеренная глубина иглы |
| stacked leave tip-gate | история `stitches.ts` `sewKagariLegs`, удалён в `c540039` | прежняя эвристика выхода, не возвращать как действующее правило | отсутствие заранее оставленного коридора не разрешает расщеплять прежние нити. GT16 велит их раздвигать; GT14 описывает приглаживание ушком иглы. Прокол на прежней нити — открытый дефект #105 |
| глубина S8 / upper | `s8-kiku.ts` 1,2 мм; upper `6r=2,13 мм` | **инженерные образцы**, не фото | лабораторные piercing/buried, не обычный studio renderer |
| шаг ряда кику | `patterns.ts` `kikuSpec` `pitch = 1 × thread` | **craft** (TemariKai: верхний стежок «about 1 thread width … below previous stitch»; ряды кладут вплотную без зазора, затем прокол); прежний 1,5× — инженерный зазор под порты, отклонён | сколько кругов влезает (`fit`); тело фланка — parallel offset |
| прокол inner/outer поздних рядов | `patterns.ts` `kikuFlank` `pierceOnMeridian` | **разные правила:** верх задан `tInner` с шагом примерно в нить (GT14). Для низа код берёт максимум прежнего положения + `stretch` и результата поиска, затем ограничивает экватором. Без пересечения поиск возвращает ближайшую точку — это не найденное пересечение укладки. Значение `stretchMm=2` — выбранный ориентир для №5, не универсальный закон | метки ring>0; утверждение о фактическом естественном пересечении требует проверки, а не одного вызова `pierceOnMeridian` |
| верхний stacked bite | `kagari.ts` `biteAcross`, `patterns.ts` `pushKikuLeg` | GT14: каждый следующий верхний стежок шире и **ниже** предыдущего; центр остаётся на уже сдвинутой к экватору метке, ширина `corner·(2+overs)`: первый верхний — обычный кагари в 2 нити (TemariKai: 1–2 мм), дальше +1 нить **в сумме** за круг — клин расширяется книзу под ~26° (GT14: «about 1 thread width wider», фото готовых клиньев; разбор мастеров 23.09; `+1 с каждой стороны` из `c2b7691` сводил проколы соседних остриёв с 4-го ряда) | охват прежних рядов; прежний poleward-сдвиг противоречил источнику и сжимал верхушки |
| подъём над пересечением под натяжением | стопка: `pile-heights.ts`, `pileParts`; старый путь: `kagari.ts` `stackBump`; #103, #93 | **исследовательские гипотезы**, не калибровка ([нить на нить](thread-over-thread.md)): 0,28 мм и диапазон 0,21–0,34 получены переносом других материалов с принятыми натяжением ~1 Н и податливостью основы; показатель `m^0,85` не установлен источниками для перле. `m` — число нитей под верхней. При заданных Δ=0,28 и R=38,2 мм идеальная гибкая нить без трения над жёсткой опорой даёт касательный масштаб ~4,63 мм; это не универсальный профиль. Несколько контактов требуют расчёта целого пролёта: ни сумма, ни простой максимум бугорков не достаточны. В коде: стопка — высота сечения 0,355 мм, линейно; `stackBump` складывает горки, `midW` ≈ 1,6 мм | высота клина зависит от контактов, сечения и смещения прежних нитей. Нулевой рост, обязательное плато и предел 3–5 высот не доказаны; пересечения и неверные проколы остаются дефектами независимо от высоты |
| нырок в прокол в режиме стопки | `stitches.ts` `sewKagariLegs` (`flat`), `pileParts`; `pile-heights.ts` | рука верхнего стежка лежит поверх прежних кругов до своего порта и уходит в прокол круто, в пределах толщины нити; нить «внутри намотки» только когда её сечение целиком ниже поверхности мари, а пока хоть часть над мари — она лежит на стопке и держит поздние нити на своей настоящей высоте | рука видна с обеих сторон до порта, одна сторона не зарывается от крестика; сжатие и вдавливание клина в мари не моделируются (#93) |
| park→resume между рядами | `patterns.ts` / `KagariTrace.resume` | GT14 паркует A на время B; Toolkit велит перейти в новый ряд **до последнего верхнего стежка**, перенеся рабочую нить поверх прежнего стежка | тот же рабочий конец сразу продолжает первый surface-пролёт нового ряда; скрытой длины между рядами нет |
| какие именно цвета у наборов A и B | `jiwari.ts` `kikuThreads` | **наш подбор**: GT14 требует две нити, но hue не называет | вид цветка |

**Устарело (удалено из кода, не возвращать как факт):** `REVERSE_PORT_MM`, `REVERSE_CROSS_LIFT`, `INNER_LEAVE_LIFT` / `INNER_LEAVE_WIDTH` — прежние студийные константы; актуальны `REVERSE_JOIN_MM` / `REVERSE_HUG_MM` / `scoopRadius` / `lift0`.

**Прямой проход:** отдельный [единичный контроль](../spec/needle-channel.md) строится в `single-needle-catch.ts`; он не заменяет проколы студии или GT14. Прежний план замены гладкого `descend` сохранён в [HANDOFF](../HANDOFF.md#инвентарь-чисел-для-прямого-прокола-иглы-2209-до-реализации). Геометрия, баланс материала, нерешённая механика и ремесленная приёмка имеют отдельные статусы.

**Из источника:** [гейдж](https://www.temarikai.com/ResourcesPages/threadgauges.html) перле №5 — 7 нитей = 0,5 см, №8 — 2 нити = 1 мм (ориентиры укладки ~0,71 / 0,5 мм, зависят от натяжения; не измерения круглого диаметра или высоты); ориентир растяжки нижнего острия `1–2 мм` (GT14, Toolkit прямо говорит, что он непостоянен; у нас выбран `stretchMm = 2`); внутренние метки `5 мм` от полюса и нижние на трети пути от экватора (GT14); две рабочие нити, A шьётся и паркуется до B (GT14); S-крутка перле (needlery.org).

**Разметка не имеет универсальной толщины относительно вышивки:** [Marking Tips](https://www.temarikai.com/HowToPages/markingtips.html) допускает perle №8/12 и №5, а также металлические нити, в том числе lamé с gauge, близким к №5. «Обычно тоньше» — рекомендация выбора, не постоянный коэффициент. Конкретная пара материалов и её объёмные сечения пока не откалиброваны.

**Концы и метраж:** park/resume не добавляет скрытого хода: это тот же
необрезанный рабочий конец (GT10: `leave your thread hanging`). Для
start/finish TemariKai описывает распространённый скрытый проход 2–5 см,
часто с обратным ходом; это диапазон способа, а не одна константа и не готовый
множитель расхода. Реализация должна хранить фактические участки пути; пока их
нет, прогноз не прибавляет выдуманные сантиметры.

## 2. Отклонено, и какой цифрой

| Что отклонено | Чем измерено |
|---|---|
| открытый U в нижних точках C8 (`44a297e`) | противоречит фотографиям uwagake chidori — [spec/needle-route.md](../spec/needle-route.md) |
| длина без изгибной жёсткости (`e541872`) | на 12 точках `rκ ≈ 39` при «сходимости» решателя |
| покрытие ранее уложенной нити капсулами | немонотонно двигало перехлёст у верхнего острия; заменено точной трубкой |
| сжатие нити коэффициентами (0,18 диаметра; сечение 0,88; бугорок 2 диаметра) | три числа с потолка — откачено, [#93](https://github.com/newYurk/temari/issues/93) |
| подъём по фактической высоте нижней нити | 512 проходов сквозь против 281 |
| плато «еду поверх всей пачки» | 292 против 281 |
| последовательный решатель контакта | налезаний меньше (396 против 459), но порядок «поздняя поверх ранней» ломается втрое чаще: 88 против 26 |

Три последних — [#94](https://github.com/newYurk/temari/issues/94); там же полные замеры (`scripts/measure-render-crossings.mts`).

## 3. Чего источники не дают

- **Глубину прокола и удержание намоткой.** Фотографии показывают ход иглы, но не то, что под нитью — третья колонка таблицы в [spec/needle-route.md](../spec/needle-route.md).
- **Крутку и сжатие перле.** Ни DMC, ни Anchor, ни ткацкие поставщики, ни литература по консервации не публикуют; искали по-английски, по-французски и по-японски.
- **Натяжение и равновесие ряда.** Силовой модели нет; утверждать равновесие нельзя — [rules.md](../src/components/temari/rules.md) §6.
- **Цвета наборов.** GT14 называет их Color A и Color B и требует две нити, но какие — выбор мастера.
- **Рецепты C8 и C10.** GT55 не превращается в шесть кику ([spec/craft-sources.md](../spec/craft-sources.md) §4), GT41 — не основание для двенадцати одинаковых цветов.

## Как этим пользоваться

Число из таблицы 1 менять можно — но тогда переприниманием то, что от него зависит, и правим строку здесь. Число из «источника» менять нельзя без нового источника. Появилось новое допущение — строка сюда в том же коммите, иначе оно растворится в тексте коммита и всплывёт через месяц как «факт».

Карта ссылок между документами собирается из самих файлов: `node scripts/docs-graph.mjs` (страница в игнорируемой папке `screenshots/docs-graph/`).
