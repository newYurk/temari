# Темари — техническое ведение и следующий этап

Обновлено 16 сентября 2026 года. Рабочий проект: `/Users/newyurk/Desktop/Home/Projects/temari`.

## Текущий этап: исправление нижнего подхвата

Пользовательская проверка выявила ремесленный дефект опубликованного снимка `44a297e`: нижние точки C8 показывали открытый U вместо обратного подхвата с перехлёстом наружных ветвей. Сверка первичных фотографий подтверждает для выбранного обычного **uwagake chidori kagari**: вход иглы на стороне `+q`, скрытый ход к `−q`, затем выходящая ветвь поверх входящей. `q` задаётся местной разметкой и направлением обхода. Основания и конкретные фотографии — [spec/needle-route.md](spec/needle-route.md). Старые численные и браузерные проверки C8 проверяли заданную геометрию, но не обнаружили эту ошибку маршрута; они не считаются подтверждением правильной кику.

В рабочем этапе создан отдельный `lower-kagari.ts`: один нижний подхват на сфере, конечная физическая дзивари, обратный проход через основу и поднятый исходный выход с единственным перехлёстом над входящей ветвью. Рабочий радиус 0,2 мм, полуширина подхвата 0,6 мм, глубина 1 мм и все подъёмы — инженерные параметры. Глубина и удержание намоткой заданы условиями, а не восстановлены по фотографии. Шесть тестов fixture проверяют направление, G1, единственный конечный перехлёст, толщину, поворот, масштаб и C180/230/360 при обоих обходах; это проверка исходной конструкции, не рассчитанной формы после затягивания.

Пространственное численное ядро `spatial-contact.ts` интегрировано с наружной ветвью в `computed-lower-kagari.ts`. Расчёт **отклонён**: на 10 контрольных точках полный путь проходит проверки, на 12 при сходимости оптимизатора нарушается регулярность трубы (`rκ≈39,44`); длина меняется на 0,00368 мм. Отдельный регресс запрещает принять удачную грубую сетку вместо провалившегося уточнения. Постановка и ограничения — [spec/spatial-contact.md](spec/spatial-contact.md). Следующий математический шаг — регулярность параметризации и ограничение кривизны либо обоснованная изгибная модель; косметическое сглаживание не подходит.

В `lab.html#spatial-catch` показаны исходная исправленная схема, вход/выход, источники и явно отклонённый кандидат. Старый C8 помечен как эксперимент с неверным нижним ходом. Изолированный нижний пример **не перенесён в C8 или Simple 8**. Ранее подготовленный `spatial-catch-fixture.ts` только извлекает нынешнюю инженерную геометрию верхнего шага 10 и не подключён к мастерской; исправление нижнего маршрута имеет приоритет перед этим переносом.

Проверки этого этапа (после `44a297e`): **202 теста**, typecheck, production build; `check-lower-kagari.mjs` и `check-pages.mjs` прошли без браузерных ошибок. Просмотрены верхний/боковой/непрозрачный виды, переключение исходного и отклонённого кандидата, страницы при 1280/390/320 px. 32 SVG документации сохранены побайтно, зеркала синхронизированы. Кадры — игнорируемый `screenshots/lower-kagari-2026-09-16/`. Точный опубликованный снимок определяется `build.json` и Pages Actions; результаты публикации фиксируются в связанных issues. Художественная и ремесленная приёмка остаётся за пользователем/мастером.

## Договорённость

Пользователь передаёт Codex техническое ведение: исследование ремесла, архитектуру, реализацию и проверки. Главный критерий — воспроизводимость настоящей вышивки темари. Красивое изображение само по себе не подтверждает правильность процесса. Архитектура и прежние решения могут пересматриваться. Художественное впечатление и удобство оценивает пользователь.

Работа начата в ветке `codex/kiku-foundation`. Исходный код GitHub `cccc814` объединён с локальным обзором `3dd8a70`; общий снимок — `55584c7`. История сохранена. Новое проектирование не обязано повторять старую архитектуру; решение о полном переписывании пока не принято.

Пользователь разрешил сразу коммитить и публиковать законченные проверенные изменения. PR #91 с моделью и документацией включён в `main` (`d0a216d`). Первый срез реализации включён в `main` коммитом `a919cdb`; отдельное разрешение на каждый коммит/публикацию не требуется.

Публикация `a919cdb` подтверждена [Actions](https://github.com/newYurk/temari/actions/runs/35118845624) и `https://newyurk.github.io/temari/build.json` (чистые исходники). Браузерный проход на живом сайте завершён без ошибок: [образец C8](https://newyurk.github.io/temari/lab.html), доступность рецептов, Simple 8 A1/B1, узкие экраны и `/design/`. Обновлены задачи #36/#49/#61/#65/#79/#85/#89/#90 и вехи 2/7; старые ссылки на ветку модели переведены на `main`. Задачи ремесленной и художественной приёмки остаются открытыми.

## Сечение Simple 8 и первый расчёт контакта — 16.09.2026

Срез `7c7ebf9` исправляет пользовательский случай с тонким центром и широкими наружными остриями. В `stitches.ts` убраны зависящие от мировой широты сужение/сплющивание сечения и прижатие оси к мари; лицевые треугольники трубы развёрнуты наружу. Номинальный радиус теперь совпадает с построенным круглым сечением. `studio-section.test.ts` проверяет радиус у полюса и на склоне, наружные грани и поворот контрольного пролёта вместе с заданным подъёмом. Это не исправление всего прежнего алгоритма: форма подхватов/концов ещё зависит от Y, подъёмы эвристические, контакты Simple 8 не верифицированы. На крупных кадрах сохраняется угловатость прежних подхватов; исправление радиуса не закрывает их форму.

Параллельно реализован отдельный плоский эталон `taut-contact.ts`: одна–три круглые опоры, заданный внешний обход выпуклой оболочки, идеально гибкая нить конечной толщины. Касательные и дуги определяют форму и длину; положительный виртуальный зазор служит только сравнением. Лишняя длина выводится через геометрический порт. Пространственный C8 не использует этот решатель; его подхваты по-прежнему заданы вручную. Контракт и основания — [spec/taut-contact.md](spec/taut-contact.md), литература — [docs/papers.md](docs/papers.md).

- 15 новых тестов контакта: независимые аналитические случаи, разные радиусы, три опоры, G1, физические и виртуальные зазоры, отражение/поворот/масштаб/перестановка, недопустимые порты и удалённый самоконтакт. Многоугольный оракул независимо приближается к длине сверху; 12 случаев закреплены в тесте. Неразрешённые случаи не получают `passed` и не показываются как готовая форма.
- Общая проверка: **169 тестов**, typecheck, production build. Браузерные `check-studio-section.mjs`, `check-contact.mjs`, `check-pages.mjs` и `check-thread-path.mjs`: центр/наружное острие/боковой вид Simple 8 на светлой основе, первый стежок и A1/B1, 27 сочетаний параметров контакта, сохранение двух кругов C8, экраны 1440/390/320. Кадры технически осмотрены; человеческая приёмка остаётся открытой.
- При двух опорах диаметром 0,4 мм, рабочей нити 0,4 мм и сравнительном зазоре 0,35 мм: длина контактного пути ≈5,90681 мм, сравнительного ≈6,89369 мм, разность ≈0,98689 мм. Это запас конкретного плоского маршрута, не расход вышивки или измеренная деформация пряжи.
- Публичный документ дополнен разделом «Натянутая нить», исправлены статусы Simple 8; учебные SVG сохранены. Материалы проверки — в игнорируемых `screenshots/s8-section-2026-09-16/` и `screenshots/taut-contact-2026-09-16/`.

Следующий шаг, намеченный в `7c7ebf9`, — форма вокруг конечных расходящихся пространственных ветвей и основы при сохранении заданного переплетения. После обнаружения нижнего U приоритет уточнён: сначала правильный обратный подхват и проверяемый наружный выход, затем перенос в целый узор. Плоский срез или интерполяция двух картинок этого не заменяют. Полный рецепт GT55, предварительные фигуры и композиция остаются открытыми.

## История: второй круг uwagake — 16.09.2026

Исторические результаты ниже относятся к инженерному маршруту `cbe26f1`. Его нижний подхват признан неверным для выбранного стежка; матрица зазоров не проверяла отсутствующий наружный перехлёст.

Срез `cbe26f1` продолжает одну открытую нить через два круга и 16 стежков. Уже на шаге 8 нить обходит ранее уложенное начало; верхние подхваты второго круга проходят над прежними ветвями, затем возвращаются под ними и разметочной опорой. Последний подхват учитывает также начало второго ряда. Между кругами нет разреза или нового старта.

- `c8-uwagake-coupon.ts` строит инженерный путь в мм. `ThreadCrossing` задаёт конечные участки и сторону прохода, `ThreadCapture` — состав захвата и порядок всего пучка. Выходы и переходы проверяются отдельно от самого захвата.
- `thread-crossings.ts` проверяет пересечения радиальных проекций, прежнее время укладки целей, конечные окна, порядок над/под и разделение сечений. Поиск уточняется; проверка направлений производных защищает от пропуска близких корней. Это численный контроль заданных кривых, не формальная сертификация или автоматическое восстановление рецепта. Общий валидатор дополнительно проверяет непрерывность, сгибы, основу и все зазоры.
- В браузере доступны два круга, отдельный переход первого круга, увеличенный захват второго, подсветка прежних ветвей и прозрачная основа. Цвета объясняют части одной нити; просмотр не меняет геометрию или длину.
- Проверки: **151 тест**, typecheck и production build прошли. Новый образец отдельно проверен в **228 сочетаниях**: шесть центров × два обхода × C=180…360 мм с шагом 10 мм; также проверены поворот и масштабирование. Это дискретная матрица одиночного цветка, не весь непрерывный диапазон и не шесть цветов вместе. Минимальные нижние оценки зазоров в матрице: более 0,056 мм до удалённых частей нити и 0,107 мм до опор; `r·κ<0,682`.
- В срезе 35 операций, 89 участков, 16 захватов и 49 именованных пересечений, включая 13 отдельных пересечений выходов/переходов. При C230 полный путь ≈462,93 мм: 272,08 мм поверхностные пролёты, 185,26 мм подхваты/выходы, 5,59 мм полностью скрытые участки. Уточнение допуска 0,005→0,001 мм сохраняет успех и меняет длину менее чем на 0,000001 мм.
- Браузерные `check-thread-path.mjs` и `check-pages.mjs` прошли на production preview без ошибок. Проверены первый стежок, переход кругов, захват второго, совпадение последовательного и готового кадров, боковой/прозрачный вид, подсветка, центры/обходы и экраны 1440/390/320. Кадры осмотрены, находятся в игнорируемом `screenshots/uwagake-row2-2026-09-16/`. Это техническая визуальная проверка; человеческая приёмка остаётся открытой.
- Размеры подхватов, подъёмов и скрытых путей заданы вручную. Восемь локальных опор неподвижны. Равновесная форма натянутой нити, трение, сжатие и реальные закрепления пока не рассчитаны; положительный зазор не означает силового контакта. Старый Simple 8, основание GT55 и совместная композиция этим образцом не реализованы.

## История: непрерывная рабочая нить — 16.09.2026

Срез `d72bdc0` реализует следующий шаг в отдельном [образце C8](https://newyurk.github.io/temari/lab.html): один круг, одна открытая нить, восемь пролётов и подхватов, раздельные скрытые начало и конец. Контракт `thread-path.ts` связывает 19 операций с 29 участками. Подхваты проходят поперёк именованных коротких разметочных опор; скрытые участки участвуют в проверках и длине.

- `c8-thread-coupon.ts` строит кривые в мм. Диаметры 0,4/0,16 мм, метки 5/20 мм, глубина и форма подхватов/концов — инженерные допущения. Восемь опор неподвижны; остальные окружности C8 — идеальные направляющие, не физическая полная разметка.
- `thread-geometry.ts` проверяет единую нить и граф операций, стыки и ориентированные касательные, сгибы, объёмы основы/коридоров, зазоры и пересечения с учётом ошибки аппроксимации. Проверяются также удалённые части соседних участков, малые петли внутри сегмента, опоры между собой и с основой. Недостаточная точность даёт `unresolved`, а не успех. Кривизна и длина вычисляются численно; формальная сертификация и силовое равновесие не заявляются.
- Three получает рассчитанную центральную линию и физический радиус. «Путь внутри» меняет прозрачность основы; «Подхват крупно», боковой вид и вращение меняют камеру. Последовательный показ восьми стежков даёт тот же кадр, что и прямой показ готового круга.
- Проверки: **130 тестов**, включая 14 независимых геометрических случаев и **228 сочетаний** шести центров, двух направлений и обхватов 180…360 мм с шагом 10 мм. Дополнительно проверены совместный поворот и согласованное масштабирование. Typecheck и production build прошли. `scripts/check-thread-path.mjs` проверяет первый стежок, круг, вид сбоку/изнутри, центры, неизменность длины при просмотре и экраны 1440/390/320. `scripts/check-pages.mjs` сохраняет регрессии основной мастерской и документации. Ошибок браузера нет; кадры в `screenshots/thread-path-2026-09-16/`.
- Для C230 полный путь образца ≈177,97 мм: 125,91 мм пролёты, 46,47 мм проколы/выходы, 5,59 мм полностью скрытые части. Это не расход покупки. Концы фиксированы граничными условиями; способ и прочность закрепления, трение, сжатие и натяжение не рассчитаны. Положительный зазор — разделение нитей, не доказанное силовое касание.
- Следующий ряд с захватом прежнего пучка, основание и полная GT55, соседние цветы пока не реализованы. Старый Simple 8 этим срезом не исправлен. Художественная и ремесленная приёмка остаются отдельными.

## Панель и булавки — 16.09.2026

Срез `beac599` отвечает на пользовательскую проверку главной мастерской: значки разметки были непонятны, а булавки без сетки невидимы. В панели теперь подписанные S8/C8/C10 и реальные миниатюры окружностей, справка со схемами и назначением временных меток. Кику предлагает явные следующие действия. Ссылка на документацию на титуле называется «как устроена вышивка».

- «Булавки» ставят/снимают метки, не добавляя линий. Без сетки нет привязки к невидимым узлам. Нажатие по приподнятой головке обрабатывает конкретную булавку; медленное перетаскивание определяется по полному смещению и не вызывает случайный клик.
- «Эскиз» сохранён как отдельное соединение существующих меток, без притязания на ремесленный рецепт. Отмена работает для меток и линий. Старое скрытое рисование слотов кику в свободном режиме отключено.
- «Без сетки» переводит в свободные метки и очищает несовместимое состояние; «Эскиз» без сетки не создаёт скрытые булавки. Ручные действия ждут завершения автоматической разметки. Для выбора кику нужна готовая S8; девятая метка не переключает инструмент молча.
- Проверки: 108 тестов, typecheck, сборка. `scripts/check-pins.mjs` проверяет настоящие клики по шару/головкам, отмену и медленное вращение; `scripts/check-workbench.mjs` — подписи, справку с Escape, причины недоступности и размеры 1280/390/320. `scripts/check-pages.mjs` сохраняет проверку C8-образца и Simple A1/B1. Кадры в игнорируемых `screenshots/workbench-2026-09-16/`, `screenshots/pins/`. Художественная приёмка остаётся за пользователем, задача #14 открыта.

## История первого среза ядра — a919cdb

- В каталоге есть рецепт полюсной кику для Simple 8; `kikuRecipe` возвращает `null` для C8/C10. Наличие этого рецепта не равно полной ремесленной верификации Simple 8.
- `motifSupport` явно различает поддерживаемый рецепт и отсутствие реализации. `compileKiku` для C8/C10 теперь отклоняется через `UnsupportedPatternError`, запасная геометрия удалена. Хоси/хиси/оби тоже недоступны; свободные стежки сохранены.
- Новый `local-marking.ts` получает реальные лучи всех центров S8/C8/C10, объединяет нормали modulo ± и работает с явным обхватом в мм. Старое отображение и метраж C10 пока используют прежние 16 записей; новый адаптер получает 15 уникальных окружностей.
- В `a919cdb` `lab.html` показывал геометрический C8-образец: восемь меток, одна группа, четыре внешних острия, ещё без подхватов и физической нити. Этот этап заменён срезом `d72bdc0`, описанным выше. Полного основания GT55 по-прежнему нет.
- При смене разметки сбрасываются неподдерживаемый мотив и история старых булавок. «Пример» всегда начинает Simple 8 с севера и чистого плана.
- В `stitches.ts` обработка полюсной области зависит от мировой координаты Y. Для одинакового цветка на боковом центре C8 это требует отдельной проверки и переработки.
- Проверки первого среза: Node 24.19.0, 99 тестов, typecheck, сборка из исходников. Браузер: отказ кику C8/C10, последовательность Simple 8, образец C8, экраны 1440/390/320 и короткая ссылка документации. Это техническая проверка, не полная ремесленная приёмка. Воспроизведение: `npm test`, `npm run typecheck`, `npm run build`, `node scripts/check-pages.mjs` при работающем preview. Кадры локально в игнорируемом `screenshots/local-geometry-2026-09-16/`.
- [Предыдущий обзор](reviews/craft-simulation-review-2026-09-15.md) относится к указанным в нём старым снимкам. Его численные выводы нельзя автоматически переносить на текущий код.

## Решения этапа модели

- [Ремесленные основания](spec/craft-sources.md): GT14 остаётся контрольным Simple 8. Проверенный C8-источник — GT55, Ginny T.; его цветочный слой содержит одну группу с четырьмя внешними остриями на восьми лучах и зависит от основания из ромбов/треугольников. Это не шесть копий GT14.
- [Модель вышивки](spec/embroidery-model.md): разметочный граф и реальные лучи, рецепт, операции и идентичность рабочих нитей, пространственные пути и контакты, затем рендер. Общий компилятор допускается; поправки относительно мировой Y не входят в целевую модель.
- Начальное приближение — геометрическая укладка с ограничениями контакта и явно подготовленная круглая мари. Механическое равновесие, динамическое натяжение и физические параметры сжатия ещё не обоснованы.
- Исходный аудит выявил запасные параметры без рецепта C8, мировой базис, контакты только внутри одного центра, 16 записей вместо 15 уникальных окружностей C10, фиксированную калибровку C24. Fallback удалён; новый адаптер решает локальные лучи и единицы. Остальные ограничения старого пути ещё действуют. Численные свидетельства и команда диагностики находятся в модели.
- Полная перепись приложения не выбрана. Переход идёт через адаптеры существующих операций и контрольные примеры.

## Первый результат реализации: кику C8

Порядок: контрольный Simple 8 → один технический образец C8 → полная C8-композиция с её зависимостями → один центр C10 → полная C10-композиция. «На всех вариантах» пока означает эти три семейства проекта; другие Simple N и дополнительные разметки расширяют каталог по мере проверки рецептов. Одиночный образец C8 с тестовыми опорами не называется готовым GT55.

1. Формализовать выбранный источник C8: метки, локальные лучи, предусмотренные группы, подхват, развитие рядов, остановку и подготовительные слои. Сохранять отдельно неизвестные глубины, контакты и переходы. Масштабирование Simple 8 на шесть точек не считается рецептом.
2. Зафиксировать совместимость рецепта и разметки. Неподдерживаемое сочетание должно явно возвращать отсутствие поддержки; интерфейс не должен предлагать его как готовую кику.
3. Отделить рецепт от положения цветка. Ориентацию выводить из реальных линий дзивари у выбранного центра. Пространство для роста задавать геометрией соседей и выбранной композицией, а не общим пределом в половину сферы.
4. Описать непрерывный путь каждой рабочей нити: видимые участки, входы/выходы, подхваты, продолжение рядов, закрепление и завершение. Каждое заданное перекрытие должно следовать порядку шитья.
5. Проверить один цветок численно и визуально, затем проверить взаимодействие соседних цветков. После этого расширять поддержку на C10 с собственным рецептом.

## Архитектурное направление

Разделить четыре ответственности: геометрия разметки → рецепт действий мастера → путь нити с учётом толщины и контактов → изображение и взаимодействие. Это границы ответственности, а не требование создать новый движок или большое число модулей.

Переиспользовать проверенные операции на сфере, размерные единицы, разметку, тесты и оболочку React/Three там, где они удовлетворяют контракту. Переработать зависимости от глобальных полюсов и геометрические подстановки вместо рецептов. Сначала получить проверяемую модель уложенной нити и последовательности стежков; необходимость динамической модели натяжения оценивать по конкретным нерешённым эффектам. Не называть статическую геометрию полной физической симуляцией.

## Приёмка

- **Ремесло:** выбранный рецепт прослеживается до источника; спорные параметры и допущения записаны. По шагам можно объяснить работу иглы и рабочей нити. Проверка мастером остаётся отдельным подтверждением.
- **Математика:** согласованы единицы и масштаб; точки привязаны к нужным линиям; нет необъяснимых разрывов рабочей нити; контакты учитывают толщину в рамках явно заданной модели материала. Скрытый путь внутри основы отделён от видимого.
- **Симметрия:** совместный поворот разметки, цветка и пути не меняет расстояния, порядок перекрытий и форму. Свет и камера могут менять только внешний вид.
- **Композиция:** между соседними цветками нет неописанных пересечений; границы и порядок заполнения следуют выбранному рецепту. Одинаковые центры обрабатываются одинаково.
- **Изображение:** кадры первого стежка, A1, B1, следующего ряда, заполнения и бокового ракурса сверены с рассчитанным состоянием. Эстетическая приёмка — за пользователем.
- **Метраж:** отдельно длина уложенного пути и запас на рабочие концы; нет выдачи длины видимых дуг за точный расход покупки.

## Источники, задачи и незакрытые вопросы

- [TemariKai: стандартные разметки](https://www.temarikai.com/HowToPages/standarddivisions.html) — определения Simple, C8 и C10; источник геометрии разметки, не рецепт конкретной кику. Проверен 16 сентября.
- C8 GT55 выбран как ремесленный контрольный дизайн; полного рецепта в коде ещё нет. C10 GT41 исследован как пример сложности скрытых переходов, но рецепт следующей реализации C10 пока не выбран.
- Не решено: физическая калибровка материала и сжатия, точные скрытые переходы, реализация подготовительных слоёв и приёмка полной композиции. До проверки эти пункты не считаются реализованными.
- Задачи: [модель #61](https://github.com/newYurk/temari/issues/61), [Simple #36](https://github.com/newYurk/temari/issues/36), [C8 #89](https://github.com/newYurk/temari/issues/89), [C10 #90](https://github.com/newYurk/temari/issues/90), [путь #82](https://github.com/newYurk/temari/issues/82), [контакты #86](https://github.com/newYurk/temari/issues/86), [документация #49](https://github.com/newYurk/temari/issues/49), [публикация #79](https://github.com/newYurk/temari/issues/79).
- Дополнительный контекст прежнего исполнителя полезен прежде всего в виде ссылок на мастер-классы, принятых пользователем образцов и известных неудачных подходов. Полная переписка для старта не требуется.

## Режим работы и расходы

Выбранный пользователем режим — Astra: сейчас **Ultra** для обоснования модели вышивки и архитектуры; затем **High** для реализации, с возвратом к **Ultra** при сложных математических вопросах. Этот выбор заменяет прежнюю рекомендацию Medium/High. Приоритет расходов — решения, от которых зависит весь проект. Запись режима в документе сама по себе не переключает effort текущего чата.

Результат этапа Ultra: обоснованные ремесленными источниками правила стежка и переходов, геометрическая модель с явными допущениями, границы ответственности модулей и проверяемые критерии корректности. Переход к реализации на High — после фиксации этих решений; незакрытые вопросы остаются явно отмеченными.

Один небольшой результат за итерацию, целевые тесты и один визуальный проход при изменении изображения; найденные знания сохранять здесь и в исходниках, чтобы не исследовать их заново. Независимые проверки делегировать узко, без нескольких полных повторных обзоров. Точный расход на весь проект заранее неизвестен; доступность лимитов проверяется по аккаунту, без автоматической покупки кредитов.

Следующий этап: улучшить и обосновать форму уложенной нити — текущие поднятые обходы дают проверяемый порядок и зазоры, но не определяют натянутую форму. Затем перейти к реальным предварительным фигурам GT55, их контактам и закреплениям. Полный GT55 и C10 не открывать в мастерской до собственных критериев приёмки.

## Поддержание документации

Источник страницы «как устроена вышивка» — `public/design.html`. `node scripts/sync-design.mjs --write` обновляет существующие зеркала и короткий адрес; `--check` контролируется CI и Pages. GitHub Pages теперь собирается из исходников на Node 24 и публикует игнорируемый `dist/`; прежние пакеты backend/DB и сломанные команды шаблона исключены из активной сборки. `package-lock.json` фиксирует зависимости. Старые опубликованные копии оставлены исторически и не копируются в новый артефакт. `build.json` сообщает revision и признак локальных изменений.

Новые факты и статусы записываются в исходные документы вместе с изменением модели и соответствующих issues; это часть разработки, без отдельного фонового расписания. Правила — в `AGENTS.md`.

Дополнительная сверка публичного документа 16.09: убраны устаревшие подписи `cccc814`, уточнены фазы C8/C10, подготовленная мари и недоступность других рецептов. Разделены повороты нового геометрического образца и ограничения старого пути Simple 8. Примеры формулы маки пересчитаны: C24 даёт 42/96/448 кругов; устаревший `wrap-look.mjs` не представлен действующей проверкой. Учебные SVG сохранены.
