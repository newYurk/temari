# Передача работы: ветка `codex/temari-next`

Обновлено 19.09.2026 (каталог, Obsidian и модельные иллюстрации, Т8–Т11). Документ для независимого ревью перед включением в `main`. **Ничего из перечисленного не принято в `main` и не опубликовано на основном сайте.** Зелёные тесты не означают принятой работы: ремесленную и художественную приёмку проводит пользователь или мастер.

- Исходная база этапа: `e541872` (исторический снимок, 202 теста; не утверждение о текущем HEAD `main`).
- Ветка: `codex/temari-next`, draft PR [#92](https://github.com/newYurk/temari/pull/92). Основной Pages публикуется из `main`; пуши в рабочую ветку его не меняют. На ветке есть проверка документации/каталога и отдельное превью (`branch-preview.yml`).
- Обновлённые issues (комментарии с пометкой «не принято в `main`», ничего не закрыто): #86, #61, #89, #36.
- Окружение численных этапов 16–17.09: Node 24.19.0, Chrome headless. Срез каталога Т8: Node 24.21.0, Chromium headless через Playwright; в системе по умолчанию Node 20, проект требует ≥ 24.

## Сводка изменений по категориям

| Категория | Изменение |
|---|---|
| Математическая модель | `spatial-contact.ts`: вместо длины при фиксированных ручках — толстая нить: цель `∫|x′|²`, свободные длины ручек, ограничение `κ ≤ 1/ρ_min`, нижняя граница скорости, доказательная граница кривизны, NNLS-сертификат ККТ, предобуславливатель, защита гомотопии шага. Новый модуль `curvature-bound.ts`. |
| Приёмка расчёта | `computed-lower-kagari.ts`: лестница сеток выводится из `ρ_min` (41/60/79 точек для C230), флаг `resolved`, независимая доказательная кривизна, разности для всех соседних пар; статус теперь `accepted`. |
| Рецепты | Не менялись. Маршрут нижнего uwagake chidori — прежний (`spec/needle-route.md`). |
| Форматы данных | `SpatialContactInput.minBendRadiusMm` (необязателен, по умолчанию `1,25·r`); `SpatialContactOptions`: `curvatureTolerance`, `minRelativeSpeed`, `curvaturePrecision`, `maxStepThreadRadii`; новые метрики результата; `reactions[]` получили `kind`, `slack`, а `gapMm` стал `number \| null`. `spatialSplineBasis` возвращает `secondDerivatives`. `LOWER_KAGARI_DIMENSIONS.minBendRadiusMm = 0.25`. `ComputedLowerKagari`: `checks.refinements`, `resolutions[].resolved/curvature`, метрики `seedLengthMm/minBendRadiusMm/minimumSpans`; опция `spansPerBendRadius`; предел числа точек 48 → 128. |
| Зависимости | Не менялись. |
| Публикация | Не выполнялась. `vite.pages.config.ts` закрепляет dev-сервер на порту 8860 (`strictPort`) — только локальный просмотр. |
| Модель (Simple 8, `e52c07f`) | Опора `curve` в `spatial-contact.ts`: ранее уложенная нить — точная трубка своих кусков Безье. Simple 8 строит препятствия из неё вместо капсул; решатель остановки строже (10⁻⁴ / 2·10⁻⁶ / 5·10⁻⁷); окна подтверждаются перезапуском. |
| Приёмка (Simple 8, `e52c07f`) | Правило 4 Simple 8: контроль обусловленности — пробы ×2 вместо покрытия/2, пороги прежние (решение за владельцем). Правило неподвижной точки (≤ 10⁻⁴ мм, ≤ 4 перезапусков). |
| Форматы данных (нижний подхват, 17.09) | `ComputedLowerKagariOptions`: убран `obstacleToleranceMm`, добавлены `settleToleranceMm`, `maxSettleRestarts`; `resolutions[].restarts/settleMoveMm`; из `metrics` убран `obstacleToleranceMm`. `ThickRopeLadderInput.settle`, общий `settleSolve` в `thick-rope-ladder.ts`. |
| Форматы данных (Simple 8, `e52c07f`) | `SpatialSupport` получил вид `{ kind: 'curve', piecesMm, radiusMm }`, тип `SpatialCubicMm`; `S8KikuLevel.samplesPerSpan` вместо `obstacleToleranceMm`; из `S8_KIKU_DIMENSIONS` убран `obstacleToleranceMm`, добавлены `settleToleranceMm`, `maxSettleRestarts`. |
| Документация | `spec/spatial-contact.md` переписан; `rules.md`, `spec/embroidery-model.md`, `public/design.html` (+ зеркала штатным скриптом), `docs/papers.md` (8 проверенных DOI), `STATE.md` сокращён до 86 строк, хроника — `docs/journal.md`. |

## Задачи

### Т0. Организация Git
- **Запрос:** общая ветка от актуального `origin/main`, небольшие коммиты, один draft PR, без слияния в `main` и без force-push.
- **Результат:** `codex/temari-next` от `e541872`, отправлена на GitHub. Дубликатов с «2» в именах в репозитории и `.git` не найдено (есть только хеш `pages-DmPntCd2.js` и кадры `*-002.png`). Основной чекаут чистый; чужие файлы не добавлялись.
- **Коммиты:** `f824ce3` (перенос хроники из `STATE.md`).

### Т1. Устойчивость пространственного расчёта нижнего подхвата
- **Запрос:** понять, почему на 10 точках проверка проходит, а на 12 возникает перегиб; обеспечить допустимую кривизну в постановке без косметики и без ослабления допусков; проверить устойчивость, толщину, непрерывность, зазоры, порядок перехлёста; сохранить явный отказ.
- **Диагноз (измерено на `e541872`):**
  - (а) вырождение параметризации: отношение скоростей ≈1755, перегиб `rκ≈39` в свободном участке;
  - (б) некорректность задачи у порта: `rκ` в порту 0,12 → 0,20 → 0,38 → 0,56 при 10/12/16/20 точках, на 20 точках валидатор — 1,13.
- **Поведение сейчас (17.09, одна дисциплина с Simple 8):**
  - входящая ветвь — точные трубки (решение побитово то же, что с капсулами: выходящая ветвь её не касается);
  - строгая остановка и неподвижная точка (перезапуски 2/1/1).
  - **C230** принят на 41/60/79 точках: длина 8,86024 / 8,85969 / 8,85970 мм, доказанное `rκ ≤ 0,801 / 0,817 / 0,817`, разности длины 5,5·10⁻⁴ и 1,6·10⁻⁵ мм, формы 3,7·10⁻³ и 2,1·10⁻³ мм.
  - **По сравнению с мягкой остановкой** (8,86020 / 8,85967 / 8,85969; формы 2,4·10⁻³ и 1,9·10⁻³) решения сдвинулись не больше чем на 1,3·10⁻³ мм.
  - **Браузер:** `check-lower-kagari.mjs` — `accepted`, без ошибок, кадры `screenshots/lower-kagari-tube-2026-09-17/`. C180/230/360 × оба обхода приняты. Все сетки 8…128 точек сходятся и проходят полную проверку; с 32 точек ограничение изгиба активно в порту выхода (u = 0).
- **Ключевые решения:**
  - Модель толстой нити (граница кривизны + самоконтакт) выбрана вместо изгибной упругой модели: не нужен некалиброванный параметр жёсткости, а корректность задачи обоснована литературой (`docs/papers.md`).
  - Цель `E` вместо `L`: в непрерывной задаче минимизаторы совпадают, `E` убирает вырожденность параметризации.
  - Порог 0,8 строже прежнего допуска валидатора `< 1`, который не менялся.
  - Лестница сеток выводится из физического масштаба `ρ_min`, а не подбирается; недостаточно разрешённые сетки не принимаются даже при зелёных проверках.
  - Сертификат ККТ через NNLS: при плотных активных ограничениях кривизны нарушается LICQ, и множители метода Лагранжа расходятся.
  - Защита гомотопии: без неё предобусловленный шаг на 8 точках «перепрыгнул» столб в эталоне капсулы.
- **Допущения:**
  - `ρ_min = 0,25 мм` — инженерное, не измерено, и форма у выхода от него зависит.
  - Прежние допущения фикстуры сохранены: глубина 1 мм, радиусы, запас 0,003 мм.
  - Изгибной жёсткости, трения, сжатия и удержания прокола в модели нет.
- **Коммиты:** `5a219e2` (доказательная граница кривизны), `9d9e474` (постановка и приёмка), `85b94d2` (документация), `+` коммит сценария исследования и этого файла.
- **Проверки (снимок `85b94d2`):**
  - 213 тестов (было 202; +6 границы кривизны, +3 решателя, +2 приёмки; изменённые тесты описаны в `spec/spatial-contact.md`), typecheck, production build.
  - `check-lower-kagari.mjs`, `check-thread-path.mjs`, `check-pages.mjs` на production preview — без ошибок.
  - `design.html` проверен на 1280/390 px, `sync-design --check` зелёный.
- **Смысл новых тестов:**
  - аналитические эталоны: капсула 4+π со сходимостью, полуокружность Дубинса с теоремой `L ≥ πr/rκ_max`;
  - второй локальный минимум («шпилька»);
  - независимый лагранжиан конечными разностями, включая множители кривизны;
  - доказательная граница против независимой выборки;
  - зеркальная ковариантность принятого стежка;
  - прежние сетки 10/12 проходят, но помечены недостаточно разрешёнными.
- **Воспроизведение:**
  ```
  npm test && npm run typecheck && npm run build
  npx tsx scripts/study-spatial-convergence.mts 8,12,24,32,40,48,64,80,96,128 230 1
  npx vite preview --config vite.pages.config.ts --host 127.0.0.1 --port 4174 --strictPort
  node scripts/check-lower-kagari.mjs http://127.0.0.1:4174/
  ```
  Просмотр: `lab.html#spatial-catch` → «После укладки», «Подхват сбоку», «Что именно проверяется». Кадры — игнорируемый `screenshots/lower-kagari-thick-rope-2026-09-16/`.
- **Известные ограничения:**
  - решатель локальный: топологию задаёт затравка, петлю в затравке он не снимает (`unresolved`);
  - полностью насыщенная дуга на 64 точках не укладывается в бюджет (`unresolved`);
  - разность форм оценена по 201 точке, а не сертифицированным расстоянием Хаусдорфа;
  - глобальная толщина (ropelength) отдельно не сертифицируется, самозазор проверяет валидатор;
  - округление с плавающей точкой в доказательной границе не учитывается;
  - расчёт в браузере занимает 3–4 с в web worker;
  - в Simple 8 и C8 подхват не перенесён.

### Т2. Локальный просмотр ветки (добавлено пользователем)
- **Цель:** смотреть изменения ветки локально, параллельно с другими проектами, каждый на своём порту.
- **Готово, когда:** сервер Темари всегда поднимается на одном адресе и не переезжает молча на соседний порт.
- **Результат:** `server.port = 8860`, `strictPort` (коммит `8bad8d0`). Адрес: `http://127.0.0.1:8860/` (мастерская), `/lab.html#spatial-catch`, `/design.html`. Команда: `npm run dev` из папки worktree. Занятые порты на момент проверки: 8851 — «Ролльня» (worktree), 8852 — Rollery Continuation v227.
- **Зависимость от математики:** нет. Какой проект пользователь считает третьим — уточняется; настраивать порт чужого проекта отсюда не следует.

### Т3. Легенда цветов на узком экране (независимое улучшение интерфейса)
- **Проблема:** на 320 px кружок цвета переносился отдельно от подписи и выглядел как ключ соседнего пункта.
- **Результат:** пары «кружок + подпись» не разрываются (коммит `f647e99`); проверено кадром 320 px.

### Т4. Перенос подхвата в контрольный Simple 8 — тема `codex/temari-next-s8`, слита в `codex/temari-next` 17.09 (`e52c07f`, не в `main`)
- **Цель:** отдельный стежок → круг → следующий ряд с верхним охватом пучка на Simple 8 (размещение GT14).
- **17.09, ночь, до перехода на трубки** (`36b687e`, `4bbe7b5`, `47ee7eb`):
  - неподвижная точка — перезапуск окна из своего решения, пока сдвиг не станет ≤ 10⁻⁴ мм;
  - строгая остановка: 10⁻⁴ / 2·10⁻⁶ / 5·10⁻⁷;
  - тест поворота.

  Вердикт тогда — `unresolved`: уход от верхнего острия 2,14·10⁻³ → 6,29·10⁻³ мм не сжимался. Данные — `screenshots/s8-settle-2026-09-17/`, `screenshots/s8-conditioning-2026-09-17/`.
- **Причина** (`aa08023`, исследование — `screenshots/s8-cover-2026-09-17/`). Собственная сетка ухода сходится: на замороженных препятствиях 47 разрешений отличаются ≤ 1,5·10⁻³ мм. Разницу x3→x4 создавало покрытие прихода капсулами:
  - при данных x3 шаги 6,3·10⁻³ → 4,1·10⁻³ мм с запасом 2·10⁻⁴ и 8,4·10⁻⁴ → 5,3·10⁻⁴ с запасом 5·10⁻⁵;
  - механизм: многоугольный гребень на почти нейтральном равновесии, сдвиг ~ `(N|H″|/k)·ℓ/2`;
  - мелкое покрытие не решение: у нижних окон 310–622 капсулы при бюджете 256.
- **Замена** (описана до реализации):
  - `be9c179` — опора `curve` в решателе: точное расстояние до кусков Безье, дерево коробок и граница по хорде, эталонный путь побитово;
  - `28a6ad3` — Simple 8 строит препятствия из собственных кусков нити;
  - правило 4 (обусловленность): вместо «покрытие/2» — «пробы ×2», пороги прежние.
- **Результат** (`screenshots/s8-tube-2026-09-17/`):
  - **стежок и круг приняты** (острие 0 — оба обхода, острие 2);
  - уход от верхнего острия: 1,28·10⁻² → **6,6·10⁻⁴** мм; худшее последнее уточнение 1,72·10⁻³ мм (`departure-7`);
  - пробы ×2 сдвигают окна ≤ 1,7·10⁻⁴ мм;
  - поворот до 10⁻⁶ мм, зеркало побитово;
  - контрольный скан ухода до 253 пролётов ≤ 1,5·10⁻³ мм.
- **Проверки:** 238 тестов (+5 на опору `curve`; отказы добавлены в прежний тест; эталонный тест с реальным входом идёт около 45 с), typecheck, build, `check-s8-kiku.mjs` (production preview на 4174):
  - оба этапа `accepted`, ошибок в браузере нет;
  - прогон 7 мин 47 с, тайм-аут ожидания круга поднят до 30 мин;
  - кадры четырёх видов, шаг 1, «внутри основы», ширины 390/320 — `screenshots/s8-kiku-tube-2026-09-17/`.
- **Цена:** стежок около 2,5 мин, круг около 12 мин в Node под нагрузкой. Поиск ближайшего куска переписан на скаляры, результаты побитово те же.
- **Независимая рецензия** (агент, 17.09): дефектов корректности нет.
  - Исправлено по замечаниям: порядок при точных ничьих и запас на округление в дереве (регрессионный тест с желобом), замороженный реальный вход с трубками в эталонном тесте, число проб уровня из параметров решателя.
  - Записано: правило 4 слабое — ловит в основном смену точки KKT (решение владельца); одна опора `curve` на две ветви (желоб) даёт `unresolved`.
- **Не сделано / дальше:**
  1. Нижний подхват C230 (`computed-lower-kagari.ts`) всё ещё покрывает входящую ветвь капсулами. Перевод на трубки — отдельный шаг с повторной приёмкой.
  2. Совместное равновесие прихода и ухода: приход неподвижен, когда на него ложится уход. Незаконченный прототип без замеров (17.09) — патч к `07676ca` в игнорируемой `screenshots/solver-prototypes-2026-09-17/` этой рабочей папки, только локально.
  3. Тест-мутант для `cover()` в проверке перехлёстов (два корня в разных кусках обеих цепочек); заготовки мутантов — скретчпад сессии, публичного API для них нет.
  4. Следующий ряд с охватом пучка (`rowAdvanceMm` 0,45), затем C8.

### Т4б. Второй ряд uwagake — тема `codex/temari-next-uwagake2`, слита в `codex/temari-next` 17.09 (не в `main`)
- **Описание до реализации:** `b655bf2`.
  - Нижний стежок второго ряда на 3 мм дальше: наименьший чистый сдвиг для нити без сжатия, округлён.
  - Верхний стежок — ±1,0 мм и на 0,45 мм ниже, под всем пучком.
  - Поздний ряд поверх раннего.
- **Реализация:** `cc092e1` — этап `row2`, стежки заданы парой «острие, ряд».
- **Первый результат** при полном перестроении каждого уровня — `unresolved`.
  - Длина верхних окон второго ряда на шаге x2→x3 менялась на 6·10⁻³ мм.
  - Причина: x2 первого ряда стоит в другой точке KKT. Собственная сетка второго ряда сходится.
- **Решение владельца — вариант 1** (`1472a65`): первый круг — уложенная нить, принятая своей лестницей; лестница второго круга уточняет только его окна поверх x4 первого круга; каждый уровень проверяется как вся нить.
- **Рецензия** — два раунда, 82 + 22 агента. Исправлено в `ccb02f5` и `e69f47f`:
  - одинаковые опоры разметки во всех этапах;
  - верхние стежки второго ряда объявлены под окнами первого круга, валидатор это подтверждает;
  - изменённые допуски, зазоры, затравки и параметры решателя — только диагностика;
  - `combineS8Rounds` и фильтры покрыты тестами;
  - ранний отказ плана за горизонтом проекции;
  - страница не показывает цифры чужого этапа и не маскирует ошибки.
- **Результат (`ccb02f5`): `row2` принят** — острие 0 в обоих обходах и острие 2, около 66 мин на вариант.
  - Последние уточнения второго круга: длина ≤ 2,3·10⁻⁴ мм, форма ≤ 1,6·10⁻³ мм, чувствительность к пробам ≤ 1,7·10⁻⁴ мм, `rκ ≤ 0,816`.
  - Данные — `screenshots/s8-row2-opt1-2026-09-17/`.
- **Проверки:** 242 теста, typecheck, build.
- **Браузер:** `check-s8-kiku.mjs`, production preview, этапы stitch/round/row2, `e69f47f`, около 50 мин.
  - Все три этапа — «Принято», ошибок нет.
  - У второго ряда 12 строк уровней: у каждого круга свои пять и контроль. Во втором круге видно, что первый взят с его ×4.
  - Ширины 390/320 без горизонтальной прокрутки.
  - Кадры — `screenshots/s8-kiku-opt1-2026-09-17/`.
- **Дальше:**
  1. Ремесленная оценка второго ряда (плотность у нижнего острия — модель без сжатия).
  2. Сжатие пучка, третий круг, цвета наборов A/B GT14.
  3. C8.

### Т7. Мастерская: быстрый сбор кику и разбор аудита интерфейса (по просьбе владельца 17.09)
- **Запрос владельца:** «я хочу собрать кику и выбрал полюс — просто повернув к себе, нажал, и всё за меня разметили»; булавки расставлять каждый раз невозможно; часть кнопок до сих пор неочевидна.
- **Быстрый старт (`2d98d40`):** кнопка «Кику здесь» — намотка считается готовой, S8 размечена, рабочие булавки кику стоят у полюса, обращённого к зрителю. Цветок на другом полюсе сохраняется. Инструмент остаётся «Булавки», чтобы случайный сдвиг не сменил рабочий полюс.
- **Аудит интерфейса:** воркфлоу `wf_33b1f525-7fb` — 251 кадр на трёх ширинах, пять независимых взглядов (первое знакомство, касания, читаемость, формулировки, доступность), каждая находка проверялась отдельным агентом на воспроизведение.
- **Исправлено (`7da3c1b`):** шар не уползает после нажатия (мёртвая зона, бросок по последним 90 мс, любой новый палец останавливает вращение, щипок не считается нажатием); нажатие у края силуэта попадает по намотанной поверхности; головки булавок крупнее и с тёмной каймой; девять меток кику видны при любом цвете намотки; шар в мастерской крупнее (поле 1,46 → 1,2); нить по умолчанию контрастна намотке; нажатие по шару больше не пришивает случайный лепесток.
- **Исправлено (`1ed7c28`):** поворот наводил полюс в мировой верх, и вся разметка ложилась к силуэту — отсюда ощущение «очень мелко». Теперь рабочий полюс поворачивается к глазам, с наклоном, как шар держат в руке.
- **Исправлено (`2a03a08`):** «Отменить» не работала весь узор — кику кладётся планом, а отмена знала только эскиз; теперь группа лепестков снимается целиком, до голых меток. Кнопка поворота подписана словом. В подсказке появился счётчик рядов («Ряд 2 из 10»).
- **Исправлено (`5da9b1a`, `9a07e47`):** под C8 и C10 сказано, что узоры для них — позже; длинную подпись пришлось сократить, потому что на 320 px она отнимала пиксель высоты шара (`check-workbench` требует 230 и поймал это уже после пуша).
- **Плотность рядов (измерено, не подкручено):** шаг ряда кику — ровно одна перле №5 (`spec.pitch = unitFromMm(0.71)`, тест `patterns.test.ts:147`), то есть ряды лежат впритык. Ощущение «редко» на прежних кадрах давал недошитый цветок: два ряда из десяти. Ничего в шаге не менялось.
- **Добавлено (`9efc099`):** «Дошить» — один шаг вместо восьми нажатий «Следующий ряд»: план растёт послойно в порядке GT14 (кай первой четвёрки, затем второй), проигрывается одной анимацией и снимается одной отменой. Кнопка видна только при обеих группах и наличии места у полюса.
- **Исправлено (`e71a564`):** «Кику здесь» на уже вышитом полюсе стирало цветок без возврата, а полюс потом просил метки, будто ничего не было. Теперь шаг записывает нить, «Отменить» её возвращает (сначала метки, потом цветок), а строка говорит, что цветок здесь уже вышит.
- **Исправлено (`09112e0`):** в дошитом цветке оставались все девять булавок. Булавки — метки, их вынимают по ходу; последний ряд полного полюса убирает свои, отмена возвращает их вместе с рядами.
- **Вид нити (`1b49e47`, issue #16):** первый шаг плана — перле. Два сложения даны рельефом на существующей трубке; развёртка идёт витками на единицу длины, поэтому длинный и короткий стежок несут одинаковую скрутку, а плитка (виток × оборот) сходится без шва. Шаг скрутки 1,8 толщины — прикидка по фотографиям, не измерение; вынесен константой `PERLE_TWIST_PITCH`, направление скрутки не выверено. Геометрия, контакты и проверки не менялись.
- **Скорость отрисовки (`81e9fb4`):** каждый стежок перестраивал весь цветок — один длинный кадр на стежок, растущий с работой (0,07 с на восьми стежках, 0,47 с на ста пятидесяти). Разложено измерением: 0,52 с на перестройку = 0,06 с разметка перехлёстов + 0,10 с пути + 0,22 с двойное слияние геометрии. Сделано: трубки хранятся по пути, из которого построены (на полном цветке все двадцать обращений — попадания); непрозрачная нить рисуется кусками, а не одной слитой геометрией (призрак по-прежнему сливается, иначе куски смешиваются сами с собой); каждая дуга опрашивается один раз за проход, а не на каждую пару. Итог: 0,29 → 0,07 с на стежок, худший кадр 0,48 → 0,15 с, медиана 60 к/с. Картинка не изменилась.
- **Перехлёсты в мастерской (`35d39c7`, `edce559`; issues #93, #94).** Владелец заметила, что нить «прыгает» через лежащую под ней и спросила, правильно ли нити ныряют друг под друга. Проверено замером (`scripts/measure-render-crossings.mts`), а не на глаз: на дошитом цветке 471 перехлёст из 1056 проходил **сквозь** (оси ближе 0,95 диаметра), порядок нарушен в 28 случаях, 178 пар соседних рядов из 320 налезают. Причина — мастерская не решает контакт, а поднимает нить оценкой.
  - Исправлено без единого нового коэффициента: точка встречи уточняется по настоящим кривым (раньше её искали с точностью 1/20 стежка при ширине бугорка 0,045), подъём над нитью стал ровно один диаметр (было 0,92 — число ниоткуда; один диаметр — это касание двух объявленных круглых сечений). Стало: первый круг 0 из 8, три круга 53 из 134, десять кругов 281 из 912.
  - **Отклонено, четыре попытки** (все с замерами в #94): подъём по фактической высоте нижней нити (512 против 281 на десяти кругах); плато «еду поверх всей пачки» (292); полноценный последовательный решатель отдельным модулем — всего налезаний меньше (396 против 459) и соседние ряды чинит (39 против 178), но порядок «поздняя поверх ранней» ломается втрое чаще (88 против 26). Причина принципиальная: отрисовка умеет только поднимать нить, а в жизни она в таких местах проходит **под** пучком. Остаток задачи — либо перенос настоящей численной укладки в мастерскую, либо принятое и записанное расхождение.
  - **Сжатие нити (#93):** пробная реализация (сжатие 0,18 диаметра, сечение 0,88, бугорок в 2 диаметра) написана и откачена — все три числа прикидка. Решение владельца: оставить круглое несжимаемое сечение, пока нет источника параметров.
- **Проверки:** 254 теста, typecheck, сборка; на собранной версии `check-workbench`, `check-pins` (настоящие нажатия), `check-quick-kiku` (в нём теперь и отмена группы), `check-studio-section` — без ошибок; `check-s8-kiku` (численные этапы, ~50 мин) прогнан отдельно. Кадры: `screenshots/after-2026-09-17/`.
- **Жалобы владельца после проверки (17.09, вечер):**
  - **Пропала пружина после сдвига двумя пальцами (`8cecbe6`).** Проверка касания из `7da3c1b` писала центр шара в тот же вектор, к которому пружина возвращает шар; R3F проверяет касание на каждом движении пальца. Теперь у проверки свой вектор, точка покоя — замороженная константа. `scripts/check-pan-spring.mjs` (два касания через CDP, 390 px): до правки шар после отпускания стоит на 0,589, после — 0.
  - **Первые четыре лепестка не того цвета (`01a8326`).** «Кику здесь» на новом шаре заменял выбранный цвет парой по умолчанию, а после первой группы палитра красила только вторую нить. Теперь пара игрока сохраняется (заменяется только нить цвета намотки), а кнопки «1» и «2» у палитры выбирают, какую четвёрку красит палитра. Тесты: 260; `check-workbench`, `check-quick-kiku` прошли.
- **Не сделано:** вид нити (перле/мулине — issue #16), титульный экран и справка по существу не пересматривались, пояс оби не начат.

### Т8. Точная совместимость каталога и карта Obsidian, 19.09

- **База:** `6c11341`, общая ветка `codex/temari-next`. Пользователь разрешил этот срез после Model Council. Код собирается в общей ветке; в `main` не принято.
- **Исправлено:** `MotifEntry.requires` заменён на свидетельство `compatibility` с точными ID и состояниями `documented / unverified / unknown`. Подстановка семейства `simple` в S8 удалена. `kiku-16` сохраняет S16 как непроверенное утверждение старого каталога, без нового рецепта.
- **Согласованность с игрой:** `PatternRecipe.divisionId`, явный адаптер `runtimeDivisionFor`, проверка идентичности собственного рецепта строки и рецепта `motifSupport`. Поддержка семьи не выдаётся за реализацию всех вариантов. S4/S10/S16 не наследуют S8.
- **Карта:** `00 Совместимость узоров.md`, отдельные подтверждённые и кандидатные обратные списки, маленькая Mermaid-схема в каждом варианте. Вики-рёбра разметки создаются только для подтверждённых требований; кандидаты остаются обычными ID. Исходники и генератор исправлены, `.md` пересобраны, рисунки владельца и существующие настройки не менялись.
- **Контракт:** [spec/catalog-compatibility.md](spec/catalog-compatibility.md), `rules.md`, `STATE.md`, исходная публичная документация и её зеркала синхронизированы. Проверки каталога добавлены в documentation CI.
- **Проверено на Node 24.21.0:** 128 целевых тестов (actions, patterns, kagari, library, catalog-compatibility, division, jiwari, stitches, studio-section); `typecheck`; production build; синхронизация публичных документов и `build-craft-vault --check` (55 заметок). Предупреждение сборки о крупных чанках сохранено; оптимизация бандла не входит в срез.
- **Регрессия относительно `6c11341`:** побитовое сравнение структур операций `compileKiku` для 45 комбинаций (полюса 0/1/оба × 1/2/3/10/fit кругов × A/B/обе группы) и двух наборов рабочих булавок без отличий.
- **Браузер:** production-документация 1280/390/320 px без горизонтальной прокрутки; мобильная мастерская 390 px: «К вышивке → Кику здесь → Начать кику → Вторая группа → Отменить», без ошибок страницы. 12 Mermaid-схем разобраны и образец с S16 просмотрен, матрица содержит 12 вариантов. Кадры: `screenshots/catalog-2026-09-19/`, не в git.
- **Предел проверки:** схемы и Markdown просмотрены в отдельном браузерном рендере, не в приложении Obsidian. Breadcrumbs/Juggl и человеческая оценка карты не приняты. Полный тяжёлый численный набор не запускался; это не повторная приёмка физики.
- **Не изменено:** геометрия, шаг рядов, сжатие, контакты, UI-действия и доступные игроку рецепты. #93/#94, полные C8/C10 и человеческая ремесленная приёмка остаются открытыми.
- **Дальше:** сверить источник конкретного `kiku-16` и переводить кандидатов в подтверждённые только с источником; отдельно оценить интеграцию контрольного S8 в мастерскую. Занятость области, предварительные слои и совместимость по локальному центру не считаются реализованными этой картой.

### Т9. Читаемое представление в Obsidian, 19.09

- **База:** `63a0efa`, общая ветка `codex/temari-next`. Повод: пользовательские скриншоты показали, что правильные метаданные сами по себе не сделали карту понятной.
- **Первый экран:** `00 Карта ремесла` сразу показывает небольшую вертикальную схему разметка → выбранный полюс → кику, затем её стежок и нить. Дублирующий H1 снят, статистика и граф механик перенесены в `00 Состояние реализации`. Карта не объявляет общей проверки композиции готовой.
- **Детализация:** схемы в узорах идут до метаданных реализации; у разметок есть отдельные связи с вариантами. Подтверждения и кандидаты не смешиваются. Большие веера разделены на небольшие схемы, поскольку в реальном Obsidian один из них выходил за правый край узкой панели.
- **Имена узлов:** четыре генерируемых заметки переименованы в `Узор · сикаку`, `Стежок · сикаку`, `Узор · судзидагику`, `Стежок · судзидагику`. Это исправляет подписи самого Graph View, а не только ссылки. Добавлены `тип`, `id` и aliases прежних имён; все известные ссылки внутри генератора разрешаются однозначно. Полные старые пути в личных, не попавших в репозиторий заметках не мигрировались; рисунки не редактировались.
- **Нативная проверка:** Obsidian 1.13.7, отдельный профиль и копия vault в тестовой Linux-среде, не пользовательский компьютер. Разрешение на этот запуск получено у владельца. 22 заметки, 38 Mermaid-схем: ширина до 368,25 px, масштаб SVG 1, обрезки в проверяемой узкой панели нет. Клики main → Simple 8 → узор сикаку → стежок сикаку прошли; MetadataCache различает новые пути, неразрешённых ссылок у контрольного узора нет.
- **Проверки кода:** 136 целевых тестов, typecheck, production build, публичные зеркала и vault `--check`. Добавлены 8 тестов представления в documentation CI. Сам генератор выдаёт 56 заметок.
- **Границы:** `.obsidian` владельца, настройки раскладки/масштаба, плагины и рисунки не меняются. Штатный общий граф не превращён в диаграмму с фиксированными координатами; читаемая подписанная структура находится внутри заметок. Проверка полного численного решателя не повторялась, физика и геометрия не менялись.
- **Приёмка:** техническая проверка не закрывает оценку удобства владельцем. Результат предлагается смотреть только после push в `codex/temari-next`; в `main` не принято.

### Т10. История ракурсов и ранние схемы дизайн-дока, 19.09

- **Запрос во время Т9:** владелица заметила неудачные «изометрические» рисунки в документе «Дизайн-док · целевая мастерская» и вспомнила, что схемы меняли.
- **Проверено по Git:** `1be197b` добавил объёмные схемы; `46164d4` сменил ракурс на вид к полюсу; `a07a13f` добавил отдельный косой крупный план и скрыл старую плоскую схему. Эти изменения есть в текущем документе, не потеряны из-за зеркал. Но ручные SVG 10.09 не стали изображениями пересчитанного пути 16–17.09.
- **Исправлено без новой геометрии:** раздел `#diagram-status` объясняет различие ракурса и правильности маршрута, ведёт к `#lower-chidori`, `lab.html#spatial-catch` и `lab.html#s8-kiku`. Ранние схемы стежка сохранены в раскрываемом архиве `#stitch-archive`; подписи больше не предлагают «внутрь» как второй режим проверенной кику. Уточнена дата редакции документа.
- **Проверки:** все 32 SVG сохранены побайтно относительно `63a0efa`; источник и зеркала синхронизированы; build/typecheck прошли. В браузере 1280/390/320 px нет горизонтальной прокрутки, архив открывается и закрывается, внутренний переход к актуальному объяснению работает.
- **Не сделано:** сами рисунки не перерисованы и не объявлены улучшенными или ремесленно принятыми. Следующая содержательная замена иллюстраций должна строиться из актуальных контрольных путей с двумя согласованными ракурсами и явными обозначениями над/под, а не из подправленных на глаз старых SVG. Отслеживается в #49.
- **Организация:** отдельный документационный коммит после среза Obsidian, в той же общей ветке, без слияния в `main`.

### Т11. Иллюстрации из одного текущего контрольного пути, 19.09

- **База:** `368ed0f`. Пользователь разрешил продолжить замену спорных рисунков согласованными видами актуального пути.
- **Источник:** новый запуск `computeLowerKagari()` с неизменёнными значениями по умолчанию. C230, радиус рабочей нити 0,2 мм, глубина 1 мм; параметры инженерного образца, не калибровка реальной перле. 41/60/79 точек: все уровни приняты, r·κ ≤ 0,816537 на последнем уровне; максимальные соседние различия длины 0,0005501 мм, формы 0,0037281 мм. Входящая ветвь и скрытый проход фиксированы, рассчитана только наружная уходящая ветвь.
- **Артефакт:** `docs/fixtures/lower-kagari-diagram.json` с геометрией, портами, проверками и SHA-256 11 зависимостей. `compute-stitch-diagram.mts` пересчитывает его, `build-stitch-diagrams.mts` строит/проверяет выделенный блок публичного документа. Устаревшая модель или отклонённый снимок не выдаются за актуальный рисунок.
- **Виды:** ортографический по нормали и косой на 55°, одинаковый масштаб и те же мировые координаты. Увеличен локальный подхват; продолжения ветвей за рамкой явно оговорены. 1/2: вход/выход, 3: уход над приходом. Цвета обозначают участки одной нити, фоновая сетка не является дзивари. Скрытый проход включается/выключается сразу в обоих видах.
- **Проверки:** 10 новых тестов проекций, непрерывности, источника, перехлёста и синхронизации; всего 146 целевых тестов. Typecheck/build, зеркала и vault прошли. Браузер 1280/390/320: подписи помещаются, два вида используют одинаковые порты/геометрию, переключатель работает мышью и клавиатурой, происхождение данных раскрывается, горизонтальной прокрутки и ошибок страницы нет.
- **Не менялось:** физическая модель, параметры решателя/материала, UI-действия мастерской, 32 прежних SVG. Это новый текущий контрольный пример, не перерисовка всех узоров и не приёмка полного цветка. Схема эксплуатации: [spec/stitch-diagrams.md](spec/stitch-diagrams.md).
- **Остаётся за человеком:** понятность объяснения и ремесленная оценка. Верхний подхват пучка, следующие ряды и вся композиция не считаются объяснёнными двумя видами нижней точки.

### Т12. Один полюс S8, две рабочие нити A1 → B1, 19.09

- **Запрос:** исправить показанную владельцем обзорную кику, выполнить первый проход двух наборов, проверить и отправить в `codex/temari-next`.
- **Модель:** фаза 0 сохраняет прежний план S8; фаза 1 начинает B на следующем реальном луче. Общие 16 физических коротких участков дзивари; независимые ID нитей и операций. B решается заново с точными трубками уже уложенной A, не поворотом её копии и не подъёмом в renderer.
- **Численная проверка:** канонические пять сеток и удвоенные пробы для каждой нити, прежние допуски. A фиксируется на x4; все пять B и контрольный B проверяются относительно этой A. Межнитевые расстояния включают скрытые хвосты, а восемь наружных перехлёстов имеют конечные проверяемые декларации B над A. Снимок с проверками и SHA-256 исходников: `public/fixtures/s8-ab.json`; подробная постановка: [spec/s8-two-threads.md](spec/s8-two-threads.md).
- **Просмотр:** `s8-ab.html` читает снимок без решателя в браузере. Шаги 0…8 — A1, 9…16 — B1; есть A1/оба набора, вид на полюс/косой/крупный, вращение и прозрачная основа.
- **Документация:** обзор в `#motifs-corrected` генерируется из тех же рассчитанных кривых. Удалены ручные звёзды с неверным направлением второго ряда, нижние метки имеют правильную проекцию GT14. Хоси названа схемой обхода, а не готовой геометрией вышивки.
- **Граница:** численная проверка только указанного образца, не матрица размеров/обходов A/B и не физическое равновесие двух подвижных нитей. Нет A2/B2, второй полюс не построен, мастерская остаётся прежней; #86/#94 и человеческая приёмка не закрываются.
- **Прежняя регрессия:** 105 тестов планировщика, геометрии, перехлёстов, каталога и нижнего подхвата прошли; прежняя модель нижнего подхвата и её генератор не изменены.
- **Итог расчёта:** A и B `accepted`, 12 полных построений (по пять сеток + удвоенные пробы), все шесть межнитевых проверок `passed`, по восемь наружных пересечений. На отображаемом x4: A = 321,547748 мм, B = 322,093696 мм; это длины контрольных нитей с заданными концами, не закупочный расход. Сертифицированные верхние границы r·κ: 0,801134 для обеих. Интервал минимального зазора A/B: [0,00127438; 0,00305598] мм; положительность не является измерением натяжения.
- **Сходимость:** максимальные разности в двух последних уточнениях: A — длина 0,0006171 мм, форма 0,0128024 мм; B — 0,0005769 мм и 0,0035814 мм. Прежние условия сжатия последовательности и чувствительности выполнены; диагностик приёмки нет.
- **Итог проверок:** ещё 15 тестов снимка/генератора и vault, всего 120 целевых тестов; typecheck, production build, оба генератора, зеркала и карта прошли. Не заявляется новый полный прогон всех тяжёлых матриц проекта. Браузер 1280/390/320: A1, первый шаг B, полная композиция, ноль/первый шаг A, все три ракурса, прозрачность, вращение и возврат, переход из схемы проверены; горизонтальной прокрутки и ошибок страницы нет. Ошибка загрузки показывает ошибку и не включает управление.

### Т13. Программа мастеров и калибровка по реальным работам, 19.09

- Во время расчёта A/B владелец задала долгосрочный сценарий: реальные мастера сравнивают расход с прогнозом, данные улучшают симулятор, затем он помогает покупать нити с обоснованным запасом.
- `c7d582c` отдельно отправлен в общую ветку: [docs/craft-calibration-program.md](docs/craft-calibration-program.md). Зафиксировано направление постоянного бесплатного доступа для участников ранней проверки; платная модель для остальных предполагается, тарифы и граница когорты не выбраны.
- Аудит отделяет имеющиеся длины контрольных путей и локальный save от отсутствующей базы фактического расхода. Описаны замороженный прогноз, паспорт материалов/рецепта, качество измерения, концы/отходы, отдельные контакты/согласия/права и проверка калибровки на отложенных работах.
- Сервер, регистрация, email-сбор, платежи и блокировки игры не включались. После явного подтверждения владельца создана [задача #95](https://github.com/newYurk/temari/issues/95), открыта, веха «v1 · вышивка»; опубликованное описание и назначение вехи сверены с согласованным черновиком.
- Первый этап #95 ограничен локальным паспортом, замороженным прогнозом, вводом факта той же области и экспортом без контактов. Один проход/полюс не сравнивается с целым изделием; неподдержанные части не превращаются в нулевой расход. Серверный пилот, проверка калибровки и совет по закупке отделены от этого этапа.

### Т5. C8 — после проверки операций Т4 и двух рабочих групп Т12.

### Т6. Ведение issues и milestones (постоянная задача, поручена пользователем 16.09)
- **Правило:** на каждом этапе агент комментирует, закрывает, переоткрывает, назначает и переносит issues между вехами. Исключения: issues с меткой `enjoy` и человеческую приёмку закрывает только человек; результат из рабочей ветки помечается «не принято в `main`».
- **Разбор 16.09:** воркфлоу `wf_2a0b1d8d-412` — 12 агентов, все закрытия и переносы прошли состязательную проверку.
  - Прокомментированы 30 issues: #1, #2, #4, #15, #16, #30, #36, #45, #49, #55–#58, #61–#67, #69, #74, #76, #79–#82, #84, #86, #88.
  - Закрыты как решённые в `main`: #67 (`056e643`), #80 (`a919cdb`).
  - Назначены владельцу, где следующий шаг за ней: #1–#5, #14.
  - `later` поставлен задачам покрытия основы #45, #63, #74 (`rules.md` §1) и #56. `enjoy` поставлен #58.
  - #56 и #58 перенесены в «v2 · практика» («поток», «тихий финал» названы в описании вехи).
  - Описания вех M2 и M7 дополнены состоянием ветки.
- **Для владельца:** все открытые задачи вехи «v0 · намотка» теперь помечены `later`; саму веху не трогал.
