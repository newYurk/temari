# Передача работы: ветка `codex/temari-next`

Обновлено 16.09.2026. Документ для независимого ревью перед включением в `main`. **Ничего из перечисленного не принято в `main` и не опубликовано на основном сайте.** Зелёные тесты не означают принятой работы: ремесленную и художественную приёмку проводит пользователь или мастер.

- База: `origin/main` = `e541872` (проверенный снимок, 202 теста).
- Ветка: `codex/temari-next`, draft PR [#92](https://github.com/newYurk/temari/pull/92). Pages публикуется только из `main` (`.github/workflows/pages.yml`), поэтому пуши в ветку сайт не меняют; на ветке запускается только проверка зеркал документации.
- Обновлённые issues (комментарии с пометкой «не принято в `main`», ничего не закрыто): #86, #61, #89, #36.
- Окружение проверок: Node 24.19.0 (в системе по умолчанию Node 20, проект требует ≥ 24), Chrome headless через Playwright из `devDependencies`.

## Сводка изменений по категориям

| Категория | Изменение |
|---|---|
| Математическая модель | `spatial-contact.ts`: вместо длины при фиксированных ручках — толстая нить: цель `∫|x′|²`, свободные длины ручек, ограничение `κ ≤ 1/ρ_min`, нижняя граница скорости, доказательная граница кривизны, NNLS-сертификат ККТ, предобуславливатель, защита гомотопии шага. Новый модуль `curvature-bound.ts`. |
| Приёмка расчёта | `computed-lower-kagari.ts`: лестница сеток выводится из `ρ_min` (41/60/79 точек для C230), флаг `resolved`, независимая доказательная кривизна, разности для всех соседних пар; статус теперь `accepted`. |
| Рецепты | Не менялись. Маршрут нижнего uwagake chidori — прежний (`spec/needle-route.md`). |
| Форматы данных | `SpatialContactInput.minBendRadiusMm` (необязателен, по умолчанию `1,25·r`); `SpatialContactOptions`: `curvatureTolerance`, `minRelativeSpeed`, `curvaturePrecision`, `maxStepThreadRadii`; новые метрики результата; `reactions[]` получили `kind`, `slack`, а `gapMm` стал `number \| null`. `spatialSplineBasis` возвращает `secondDerivatives`. `LOWER_KAGARI_DIMENSIONS.minBendRadiusMm = 0.25`. `ComputedLowerKagari`: `checks.refinements`, `resolutions[].resolved/curvature`, метрики `seedLengthMm/minBendRadiusMm/minimumSpans`; опция `spansPerBendRadius`; предел числа точек 48 → 128. |
| Зависимости | Не менялись. |
| Публикация | Не выполнялась. `vite.pages.config.ts` закрепляет dev-сервер на порту 8860 (`strictPort`) — только локальный просмотр. |
| Документация | `spec/spatial-contact.md` переписан; `rules.md`, `spec/embroidery-model.md`, `public/design.html` (+ зеркала штатным скриптом), `docs/papers.md` (8 проверенных DOI), `STATE.md` сокращён до 86 строк, хроника — `docs/journal.md`. |

## Задачи

### Т0. Организация Git
- **Запрос:** общая ветка от актуального `origin/main`, небольшие коммиты, один draft PR, без слияния в `main` и без force-push.
- **Результат:** `codex/temari-next` от `e541872`, отправлена на GitHub. Дубликатов с «2» в именах в репозитории и `.git` не найдено (есть только хеш `pages-DmPntCd2.js` и кадры `*-002.png`). Основной чекаут чистый; чужие файлы не добавлялись.
- **Коммиты:** `f824ce3` (перенос хроники из `STATE.md`).

### Т1. Устойчивость пространственного расчёта нижнего подхвата
- **Запрос:** понять, почему на 10 точках проверка проходит, а на 12 возникает перегиб; обеспечить допустимую кривизну в постановке без косметики и без ослабления допусков; проверить устойчивость, толщину, непрерывность, зазоры, порядок перехлёста; сохранить явный отказ.
- **Диагноз (измерено на `e541872`):**
  - (а) вырождение параметризации: отношение скоростей ≈1755, перегиб `rκ≈39` в свободном участке;
  - (б) некорректность задачи у порта: `rκ` в порту 0,12 → 0,20 → 0,38 → 0,56 при 10/12/16/20 точках, на 20 точках валидатор — 1,13.
- **Поведение сейчас:** C230 принят на 41/60/79 точках. Длина 8,86020 / 8,85967 / 8,85969 мм, доказанное `rκ ≤ 0,801 / 0,817 / 0,817`. Разности длины 5,3·10⁻⁴ и 1,5·10⁻⁵ мм, формы 2,4·10⁻³ и 1,9·10⁻³ мм. C180/230/360 × оба обхода приняты. Все сетки 8…128 точек сходятся и проходят полную проверку; с 32 точек ограничение изгиба активно в порту выхода (u = 0).
- **Ключевые решения:**
  - Модель толстой нити (граница кривизны + самоконтакт) выбрана вместо изгибной упругой модели: не нужен некалиброванный параметр жёсткости, а корректность задачи обоснована литературой (`docs/papers.md`).
  - Цель `E` вместо `L`: в непрерывной задаче минимизаторы совпадают, `E` убирает вырожденность параметризации.
  - Порог 0,8 строже прежнего допуска валидатора `< 1`, который не менялся.
  - Лестница сеток выводится из физического масштаба `ρ_min`, а не подбирается; недостаточно разрешённые сетки не принимаются даже при зелёных проверках.
  - Сертификат ККТ через NNLS: при плотных активных ограничениях кривизны нарушается LICQ, и множители метода Лагранжа расходятся.
  - Защита гомотопии: без неё предобусловленный шаг на 8 точках «перепрыгнул» столб в эталоне капсулы.
- **Допущения:**
  - `ρ_min = 0,25 мм` — инженерное, не измерено, и форма у выхода от него зависит.
  - Прежние допущения фикстуры сохранены: глубина 1 мм, радиусы, запас 0,003 мм.
  - Изгибной жёсткости, трения, сжатия и удержания прокола в модели нет.
- **Коммиты:** `5a219e2` (доказательная граница кривизны), `9d9e474` (постановка и приёмка), `85b94d2` (документация), `+` коммит сценария исследования и этого файла.
- **Проверки (снимок `85b94d2`):**
  - 213 тестов (было 202; +6 границы кривизны, +3 решателя, +2 приёмки; изменённые тесты описаны в `spec/spatial-contact.md`), typecheck, production build.
  - `check-lower-kagari.mjs`, `check-thread-path.mjs`, `check-pages.mjs` на production preview — без ошибок.
  - `design.html` проверен на 1280/390 px, `sync-design --check` зелёный.
- **Смысл новых тестов:**
  - аналитические эталоны: капсула 4+π со сходимостью, полуокружность Дубинса с теоремой `L ≥ πr/rκ_max`;
  - второй локальный минимум («шпилька»);
  - независимый лагранжиан конечными разностями, включая множители кривизны;
  - доказательная граница против независимой выборки;
  - зеркальная ковариантность принятого стежка;
  - прежние сетки 10/12 проходят, но помечены недостаточно разрешёнными.
- **Воспроизведение:**
  ```
  npm test && npm run typecheck && npm run build
  npx tsx scripts/study-spatial-convergence.mts 8,12,24,32,40,48,64,80,96,128 230 1
  npx vite preview --config vite.pages.config.ts --host 127.0.0.1 --port 4174 --strictPort
  node scripts/check-lower-kagari.mjs http://127.0.0.1:4174/
  ```
  Просмотр: `lab.html#spatial-catch` → «После укладки», «Подхват сбоку», «Что именно проверяется». Кадры — игнорируемый `screenshots/lower-kagari-thick-rope-2026-09-16/`.
- **Известные ограничения:**
  - решатель локальный: топологию задаёт затравка, петлю в затравке он не снимает (`unresolved`);
  - полностью насыщенная дуга на 64 точках не укладывается в бюджет (`unresolved`);
  - разность форм оценена по 201 точке, а не сертифицированным расстоянием Хаусдорфа;
  - глобальная толщина (ropelength) отдельно не сертифицируется, самозазор проверяет валидатор;
  - округление с плавающей точкой в доказательной границе не учитывается;
  - расчёт в браузере занимает 3–4 с в web worker;
  - в Simple 8 и C8 подхват не перенесён.

### Т2. Локальный просмотр ветки (добавлено пользователем)
- **Цель:** смотреть изменения ветки локально, параллельно с другими проектами, каждый на своём порту.
- **Готово, когда:** сервер Темари всегда поднимается на одном адресе и не переезжает молча на соседний порт.
- **Результат:** `server.port = 8860`, `strictPort` (коммит `8bad8d0`). Адрес: `http://127.0.0.1:8860/` (мастерская), `/lab.html#spatial-catch`, `/design.html`. Команда: `npm run dev` из папки worktree. Занятые порты на момент проверки: 8851 — «Ролльня» (worktree), 8852 — Rollery Continuation v227.
- **Зависимость от математики:** нет. Какой проект пользователь считает третьим — уточняется; настраивать порт чужого проекта отсюда не следует.

### Т3. Легенда цветов на узком экране (независимое улучшение интерфейса)
- **Проблема:** на 320 px кружок цвета переносился отдельно от подписи и выглядел как ключ соседнего пункта.
- **Результат:** пары «кружок + подпись» не разрываются (коммит `f647e99`); проверено кадром 320 px.

### Т4. Перенос подхвата в контрольный Simple 8 — следующая задача
- **Цель:** отдельный стежок → круг → следующий ряд с верхним охватом пучка на Simple 8 (GT14) с проверенным нижним подхватом.
- **Готово, когда:**
  - каждая операция воспроизводит маршрут `spec/needle-route.md`;
  - полный путь проходит валидатор с перехлёстами и захватами;
  - вычисленные наружные ветви приняты на лестнице разрешений;
  - кадры первого стежка, A1/B1, следующего ряда и бокового вида сверены с расчётом, включая мобильный экран.
- **Зависимости:** выполнена Т1. Нужна модель верхнего охвата пучка: контакт с несколькими прежними ветвями, конечные опоры. Статус — не начато.

### Т5. C8 — после проверки операций Т4.
