# Контрольная кику Simple 8: перенос обратного подхвата

16.09.2026, дополнено 17.09; тема `codex/temari-next-s8` слита в `codex/temari-next` (`e52c07f`), в `main` не принято. Модуль — `src/components/temari/s8-kiku.ts`, просмотр — `lab.html#s8-kiku`. Модель толстой нити и её решатель — [spatial-contact.md](spatial-contact.md), маршрут иглы — [needle-route.md](needle-route.md).

## Ремесленные основания

| Источник | Что взято | Тип сведения |
|---|---|---|
| [GT14](https://www.temarikai.com/PatternsPages/Simple/GT14.html) | Верхние метки в 5 мм от полюса, нижние — на трети пути от экватора к полюсу; зигзаг по чередующимся лучам | размещение из рецепта |
| [Toolkit, chidori kagari](https://www.temarikai.com/HowToPages/ToolKit/chidorikagari.html), фото 3 и 4 | При движении работы слева направо игла идёт справа налево и у нижней, и у верхней точки; законченная верхняя точка — маленький перехлёст, уход над приходом | наблюдение по фото, одноцветная пряжа |
| [Toolkit, uwagake chidori](https://www.temarikai.com/HowToPages/ToolKit/uwagakechidori.html) | Пример на Simple 8: первый круг — обычный chidori вокруг полюса; перенос нити через начало до последнего стежка круга; во втором круге нить кладут параллельно снаружи, верхний стежок берут под всем пучком, нижний — ниже прежнего острия; каждый круг примерно на толщину нити шире и ниже | текст и фото автора |

Фото не определяют глубину подхвата, угол входа нити, форму под поверхностью, силы и трение. Это инженерные значения модели.

## Построение

- **Острия** берутся из реальных лучей Simple 8 у полюса (`localMarkingRays`), чётные — верхние, нечётные — нижние. Направление работы в острие — `q = h·(c × m)`: оно указывает на следующий луч и одинаково годится для верхних и нижних точек. У изолированной фикстуры было `q = m × v`; для верхнего острия, где `v` смотрит к полюсу, знак там был бы обратным.
- **Проколы:** вход на `+q`, выход на `−q`, по 0,6 мм от метки. Нить входит в намотку и выходит из неё под углом 45°. Скрытый подхват — две кривые Безье через точку на глубине 1,2 мм. Глубина, ручки 0,8/0,8 мм и угол подобраны так, чтобы и скрытый поворот соблюдал `ρ_min` (доказанное `rκ ≤ 0,389`). Фактическая полуширина стежка по точкам, где ось пересекает поверхность шара: 0,63 мм у нижнего острия и 0,73 мм у верхнего.
- **Ветвь** между проколами — дуга большого круга через порты. Рассчитываются только окна у проколов: 8 мм у верхнего острия и 12 мм у нижнего, где угол между ветвями всего 13,6° и перехлёст растянут на несколько миллиметров. Дальше натянутая нить на гладком шаре без препятствий лежит по дуге большого круга; стыки G1.
- **Хронология:** окна считаются в порядке шитья. Всё уложенное раньше — неподвижные препятствия: целые окна, подхваты, начало и дуги.
  - **С 17.09 (ночь)** они задаются точными трубками своих собственных кусков (опора `curve`, отбор кусков в пределах 3 мм от маршрута); дуги остаются дугами.
  - **До этого** их покрывали капсулы с запасом 0,0002 мм — см. «Исследование 17.09 (ночь)». Разметка — восемь коротких физических отрезков дзивари вокруг острий, не пересекающихся у полюса; полные линии Simple 8 рисуются как направляющие.
- **Перехлёсты объявляются до расчёта:** приход над своей дзивари, подхват под ней, уход над дзивари и над приходом (цепочка кусков), возврат круга над началом. Необъявленные пересечения видимых кусков ищутся в центральной проекции на касательную плоскость полюса (корректна для полусферы полюса) и дают отказ.
- **Этапы:** `stitch` — скрытое начало у верхнего острия, нижний подхват, верхний подхват, открытый уход. `round` — семь подхватов и возврат над началом с открытым концом у входа первого верхнего стежка второго круга: он на 0,45 мм ниже и шире, `wrapHalfBite` 1 мм. `row2` — ещё и второй круг uwagake (описание и результат — ниже), открытый конец у входа третьего.

## Приёмка

1. **Каноническая лестница задана в коде до расчёта:** 1, 1,5, 2, 3, 4 пролёта сплайна на минимальный радиус изгиба. Каждый уровень — полное построение со своей хронологией.
   - **Поздние круги (с 17.09, вариант 1, решение владельца).** Ранний круг — это уже уложенная нить: его принимает собственная лестница (этап `round` целиком).
   - Лестница позднего круга уточняет только его окна. Они строятся поверх самого мелкого канонического уровня раннего круга (x4, 4 пробы на пролёт).
   - Каждый уровень всё равно проверяется как полная нить: оба круга, стык, перехлёсты, необъявленные пересечения, кривизна.
   - Этап принят, только если принят каждый круг.
   - **Почему.** При полном перестроении грубый уровень раннего круга (две точки KKT на x2) переносился в длину позднего круга. Разбор — «Результат второго ряда».
2. **Каждый уровень:**
   - все окна сошлись;
   - полная проверка нити пройдена: G1, зоны, толщина, самозазор, перехлёсты;
   - необъявленных пересечений нет;
   - доказанное `rκ < 1`;
   - скрытые участки `≤ r/ρ_min`.
3. **Асимптотика:** для каждого окна две последние разности длины (≤ 0,002 мм) и формы (≤ 0,02 мм) в допуске и сжимаются — последняя ≤ 0,75 предыдущей или ≤ ¼ допуска. Грубые уровни показываются как предасимптотические.
4. **Обусловленность:** самый мелкий уровень строится ещё раз с вдвое более частыми пробами ограничений (8 на пролёт вместо 4); окна не должны сдвинуться сверх допусков. До перехода на трубки здесь было «вдвое более точное покрытие препятствий».
4a. **Неподвижная точка (с 17.09):** сошедшееся окно перезапускается из собственного решения (множители и штраф сбрасываются), пока перезапуск не сдвинет его не больше чем на `settleToleranceMm` = 10⁻⁴ мм (0,02 допуска на ¼), не более 4 раз. Не подтвердилось — окно помечается, этап не принимается. Режим без перезапусков (`maxSettleRestarts: 0`) только диагностический.
4b. **Остановка решателя (с 17.09):** допустимость 10⁻⁴ мм, стационарность 2·10⁻⁶, дополняющая нежёсткость 5·10⁻⁷ мм. Обоснование — ниже, в «Исследовании 17.09».
4c. **Только диагностика (с 17.09):**
   - нестандартная лестница;
   - `maxSettleRestarts` не по умолчанию;
   - любые изменённые допуски, численные зазоры, параметры поиска препятствий или затравок (`S8_KIKU_ACCEPTANCE_KEYS`);
   - заданные `solverOptions`.

   Такой расчёт не может быть принят. Размеры самой кику (окружность, метки, нить, подхваты, окна, сдвиги рядов) задают модель, и приёмка относится к ней.
5. **Отказ вместо падения:** неудачный расчёт окна, превышение бюджета опор (256) или сетки (256 точек) дают `rejected`. Лестницу, отличную от канонической, можно задать только для диагностики, принята она быть не может.

## Что показало исследование (16.09)

- **Ложная тревога проверки перехлёстов.** Если корень лежит ближе ~2·10⁻⁴ мм к стыку кусков, метод Ньютона упирался в границу соседнего куска. Исправлено: Ньютон переходит через внутренние стыки цепочки, единственность доказывается на объединении конусов производных.
- **Шум представления.** У каждой капсулы прежде был свой запас, и покрытие менялось вместе с числом кусков окна. Это давало до 2·10⁻² мм разницы формы при неизменной геометрии. Теперь покрытие единое и точное: чувствительность к нему ≤ 2,6·10⁻³ мм.
- **Разные затравки** сходятся к одному минимуму; метрика формы совпадает с расстоянием Хаусдорфа в пределах 2%.
- **Нижние острия и окна без перехлёста сходятся уверенно.** Например, `departure-1`: разности формы 1,1·10⁻² → 5,6·10⁻³ → 3,9·10⁻³ → 1,8·10⁻³ мм.
- **Верхние острия плохо обусловлены.** Приход, уход и дзивари сходятся почти в одной точке за острием, и уход лежит на «плече» прихода. Сдвиг перехлёста вдоль прихода почти не меняет длину: 0,015 мм стоят 2–3·10⁻⁵ мм длины. Это сравнимо с разницей между минимизируемой энергией и длиной на конечном сплайне. В результате острия 2 и 6 (поворот на 180°, та же арифметика) совпадают побитово, а острие 4 (поворот на 90°) даёт другие разности уточнения: 2,8·10⁻² против 1,7·10⁻² мм. Построение при этом ковариантно с точностью 10⁻⁹ мм (тест). Приёмка круга правильно остаётся `unresolved`.

## Исследование 17.09: остановка решателя и неподвижная точка

Сравнение: каноническая лестница, стежок и круг, острия 0 и 2, оба обхода. Результаты — `screenshots/s8-conditioning-2026-09-17/` основного чекаута (вне git).

**Почему нужен перезапуск.** Сертификат KKT поэлементный: множители могут лежать на пробах, которые не касаются прихода (зазоры 10⁻⁶–5·10⁻⁴ мм). Из-за этого «converged» от затравки останавливался до 1,5·10⁻² мм от дискретного минимума. Острия 2 и 4 останавливались в разных точках долины, отсюда расхождение при повороте. Перезапуск из решения доходит до минимума:

| Уход острия 0, повёрнутый на 90°, против острия 2, мм | x2 | x3 | x4 |
|---|---|---|---|
| без перезапуска, прежняя остановка | 3,8·10⁻³ | 1,5·10⁻² | 3,1·10⁻³ |
| без перезапуска, строгая остановка | — | 9,3·10⁻⁵ | 9,3·10⁻⁵ |
| с перезапуском | 4,7·10⁻⁷ | 1,7·10⁻⁷ | 1,1·10⁻⁷ |

С перезапуском уходы от всех четырёх верхних острий дают одинаковые последовательности уточнения в обоих обходах. Это закреплено тестом (x3, порог 2,5·10⁻⁵ мм; без перезапуска тест падает).

**Почему строгая остановка.** Уход у верхнего острия лежит в мягкой долине: жёсткость около 0,36 T/мм при нагрузке обхвата около 0,9 T (исследование `theory`). Поэтому допуск проникновения прямо сдвигает перехлёст. Решения x4 после перезапуска при разных допусках, мм (наибольший сдвиг контрольной точки `departure-2`):

| Допустимость / стационарность | прежние 5·10⁻⁴ / 2·10⁻⁴ | 5·10⁻⁴ / 2·10⁻⁶ | **10⁻⁴ / 2·10⁻⁶ (канон)** | 10⁻⁴ / 5·10⁻⁷, бюджет 20000/100 |
|---|---|---|---|---|
| сдвиг от канона | 2,3·10⁻³ | 6,2·10⁻⁴ | 0 | 5,0·10⁻⁵ |

Прежняя остановка давала ошибку больше десятой доли допуска формы, строгие варианты между собой согласуются. Цена — время: стежок около 90 с, круг около 5,5 мин на загруженной машине (прежде 20 с и 65 с).

**Вердикт с капсулами (до перехода на трубки): стежок и круг `unresolved`.** Все окна сошлись и подтверждены перезапуском. Проверка пути, перехлёсты и `rκ ≤ 0,81` пройдены на всех уровнях. Не проходит одно: у уходов от верхних острий 0/2/4/6 разности формы 2,99·10⁻², 2,23·10⁻³, **2,14·10⁻³, 6,29·10⁻³** мм. Обе последние в допуске 0,02, но последняя не меньше 0,75 от предыдущей и больше ¼ допуска. Разности длины 1,8·10⁻³ … 8,8·10⁻⁶ мм, чувствительность к покрытию 2,5·10⁻³ мм.

- **x2 не определён однозначно.** У дискретной задачи x2 две точки KKT на расстоянии 1,3·10⁻² мм. Строгие варианты попадают в разные: шаг x2→x3 равен 2,1·10⁻³ или 1,5·10⁻², и вариант 10⁻⁴ / 5·10⁻⁷ «принимает» стежок. Такое принятие — случайность выбора точки, а не сходимость. На x3 и x4 строгие варианты совпадают до 3·10⁻⁴ и 5·10⁻⁵ мм, так что шаг x3→x4 ≈ 6,3·10⁻³ мм устойчив.
- **Прежнее «стежок принят» было везением** грубой остановки. Прежняя остановка без перезапуска принимала стежок от острия 0 и отвергала от острия 2 — это одна и та же задача.
- Нижние острия, приходы и возврат проходят с запасом: у `closing-0` последние разности 1,9·10⁻³ и 6,7·10⁻⁴ мм.

Первоначальное объяснение — «шаг x3→x4 даёт разрешение самого ухода: при препятствиях x3 смена сплайна даёт 5,2·10⁻³ из 6,0·10⁻³» — оказалось неполным. Разбор ниже.

## Исследование 17.09 (ночь): покрытие капсулами

Замороженные данные: уровни x3 и x4 канонического стежка от острия 0, окно `departure-2`, каноническая остановка с перезапусками. Скрипт сначала воспроизвёл канонический x4 побитово. Данные и скрипты — `screenshots/s8-cover-2026-09-17/` основного чекаута (вне git).

**Собственная сетка ухода сходится.** При препятствиях x4 уход решён на 47 разрешениях, от 72 до 253 пролётов. Отличие от самого мелкого — не больше 1,5·10⁻³ мм. Шаги 72→108→144 равны 1,0·10⁻³ и 4,8·10⁻⁴ мм (отношение 0,47).

**Разность x3→x4 создаёт покрытие.** Шаги 72→108→144 пролётов, мм:

| Данные препятствий \ запас покрытия | 2·10⁻⁴ (канон) | 5·10⁻⁵ | 1,25·10⁻⁵ |
|---|---|---|---|
| x3 | **6,3·10⁻³ → 4,1·10⁻³** | 8,4·10⁻⁴ → 5,3·10⁻⁴ | 1,0·10⁻³ → 4,1·10⁻⁴ |
| x4 | 1,0·10⁻³ → 4,8·10⁻⁴ | 1,1·10⁻³ → 6,5·10⁻⁴ | 9,2·10⁻⁴ → 3,6·10⁻⁴ |

При одном разрешении (144) и одних данных x4 смена запаса сдвигает уход немонотонно:
- 4·10⁻⁴ → 6,3·10⁻³ мм;
- 2·10⁻⁴ → 2,2·10⁻³ мм;
- мельче → 0,4–1,1·10⁻³ мм.

Длина при этом сходится ровно, примерно пропорционально запасу.

**Полная каноническая лестница стежка** с запасом 5·10⁻⁵ мм:
- `departure-2`: последние разности 1,18·10⁻² и **6,9·10⁻⁴** мм. Это сжатие, чувствительность к покрытию 7,5·10⁻⁴ мм (было 2,5·10⁻³).
- Этап всё равно отвергнут: у нижнего `departure-1` капсул больше бюджета — 310 при контрольном запасе 2,5·10⁻⁵.
- С запасом 1,25·10⁻⁵ результат тот же: 1,27·10⁻² → 7,2·10⁻⁴ мм, но у `departure-1` уже 371–622 капсулы.

Мелкое покрытие — только проверка причины, не решение.

**Механизм.** Цепочка капсул заменяет гладкий гребень прихода многоугольником: хорды с изломами в стыках.
- **Размер звена.** При запасе `δ` и кривизне гребня `κ ≈ 2,1/мм` звено `ℓ ≈ √(4δ/κ)`. Это 0,02 мм при каноническом запасе, мельче пролёта ухода (0,06–0,08 мм).
- **Сдвиг перехлёста.** Уход лежит на гребне в почти нейтральном равновесии:
  - `N ≈ 0,9 T` — нагрузка обхвата;
  - `N·|H″| ≈ 1,9 T/мм` — дестабилизирующая жёсткость гребня;
  - `k ≈ 0,36 T/мм` — итоговая жёсткость.

  У многоугольника наклон гребня ступенчатый. Поэтому равновесие вдоль прихода может сместиться на величину до `(N·|H″|/k)·ℓ/2 ≈ 5·ℓ/2`, то есть до сотых долей миллиметра. Фаза узлов ухода относительно изломов меняется от уровня к уровню, и уход на разных сетках опирается на разные изломы.
- **Масштаб ошибки.** Оценка убывает как `√δ`, а фактические сдвиги — немонотонно. Поэтому контроль «запас/2» слабо её выявляет. Уменьшить запас до нужного не позволяет бюджет препятствий.

Итог: прежние варианты «сгущение узлов у контакта», «лестница по длине обхвата» и «изгибная жёсткость» лечили следствие.

## Замена: гладкая трубка вместо капсул

Описание записано до реализации (`aa08023`). Реализация — `be9c179` (решатель) и `28a6ad3` (Simple 8). Результат на ветке, человеческой приёмкой не подтверждён.

- **Опора `curve` в `spatial-contact.ts`.** Это ось ранее уложенной нити: её собственные кубические куски Безье с радиусом `r + зазор`, без запаса покрытия.
  - **Расстояние** до куска точное: корни производной квадрата расстояния (степень 5) отделяются по знакам коэффициентов Бернштейна (разбиение де Кастельжо) и уточняются, концы куска учитываются. Ближайший кусок ищется по дереву ограничивающих коробок.
  - **Градиент** — единичный вектор от ближайшей точки (теорема об огибающей).
  - **Пропуск дальних опор** сохраняется: расстояние 1-липшицево.
  - **Непрерывная проверка** берёт выборку кусков с оценкой ошибки, как для дуг, и остаётся консервативной.
  - Прежние виды опор и их арифметика не меняются: существующие результаты должны совпасть побитово.
- **`s8-kiku.ts`.** Окна, подхваты и начало становятся опорами `curve` из тех же кусков, что и видимая нить. Отбор по расстоянию до маршрута — покусочный. Дуги остаются дугами. Запас покрытия исчезает вместе с бюджетом капсул; предел числа опор решателя (256) сохраняется.
- **Правило 4 (обусловленность).** Для точного представления контроль «покрытие/2» теряет смысл. Его место занимает контроль оставшегося представления — дискретизации ограничений: самый мелкий уровень строится ещё раз с вдвое более частыми пробами (`samplesPerSpan` 8 вместо 4). Пороги прежние. Это замена одного теста представления другим с теми же допусками, а не ослабление; решение за владельцем.
- **Проверки:** точность расстояния против плотной выборки, конечные разности градиента, эталонный путь `referenceEvaluation` побитово, прежние тесты без изменений. Затем каноническая лестница стежка и круга, оба обхода, острия 0 и 2.

### Результат (17.09, `28a6ad3`)

Допуски, лестница, остановка и перезапуски — прежние. Данные — `screenshots/s8-tube-2026-09-17/` основного чекаута (вне git).

| Этап | Острие / обход | Статус | Время на загруженной машине |
|---|---|---|---|
| стежок | 0 / +1, 0 / −1, 2 / +1 | **принят** | 153 с (бандл Node); 635–845 с (tsx, первая версия поиска) |
| круг | 0 / +1, 0 / −1, 2 / +1 | **принят** | 730–765 с (бандл Node, четыре расчёта параллельно) |

- **Уходы от верхних острий.** Разности формы 2,49·10⁻², 4,94·10⁻³, 1,28·10⁻², **6,6·10⁻⁴** мм. Последняя меньше ¼ допуска, так что правило выполнено и без сравнения с предыдущей; большой шаг x2→x3 (у x2 другая точка KKT) на приёмку не влияет. Разность длины на последнем шаге — 7,4·10⁻⁷ мм.
- **Худшее последнее уточнение** — у нижнего `departure-7` перед возвратом: 1,72·10⁻³ мм.
- **Проверка пути.** Все уровни: `passed`, необъявленных перехлёстов нет, доказанное `rκ ≤ 0,809`.
- **Частота проб (правило 4).** Вдвое более частые пробы сдвигают окна не больше чем на 1,7·10⁻⁴ мм по форме и 4,3·10⁻⁵ мм по длине.
- **Неподвижная точка.** Подтверждена везде: не больше двух перезапусков. Прежний сбой `x2 departure-1` у круга от острия 2 исчез.
- **Поворот и зеркало.** Круг от острия 2 повторяет последовательности круга от острия 0 до 10⁻⁶ мм, зеркальный обход — побитово.
- **Контроль вне лестницы.** Уход `departure-2` решён на замороженных трубках x4 при 72…253 пролётах:
  - отличие от 253 пролётов — не больше 1,5·10⁻³ мм, выше 230 пролётов — до 2,5·10⁻⁴;
  - пятно контакта с приходом одно (u 0,085–0,129), а не два, как с капсулами;
  - все тройки со сдвинутыми сетками дают шаги ≤ 1,9·10⁻³ мм.
- **Остаточный разброс.** Около 10⁻³ мм на средних разрешениях — это точность остановки в мягкой долине, не представление препятствий.
- **Цена.** Расчёт дольше прежнего: стежок около 2,5 мин, круг около 12 мин в Node под нагрузкой. В браузере проверка обоих этапов заняла 7 мин 47 с. Поиск ближайшего куска переписан на скаляры, выделений памяти стало меньше, но они остались.
- **Независимая рецензия** (агент, 17.09): дефектов, влияющих на корректность или приёмку, нет. Проверено:
  - ближайшая точка на 77 тыс. случаев, включая тройной корень и вырожденные куски;
  - граница по хорде;
  - 1-липшицевость на реальных опорах;
  - эквивалентность пропуска цепочек;
  - отбор кусков Simple 8.

  По её замечаниям:
  - **Ничьи.** При точной ничьей расстояний у разных кусков дерево могло выбрать не тот кусок, что эталон (разная ближайшая точка при том же расстоянии). Добавлены запас на округление и номер куска как третий ключ; регрессионный тест с желобом падает без исправления.
  - **Эталонный тест** получил замороженный реальный вход с трубками.
  - **Число проб** уровня берётся из параметров решателя.
- **Слабость правила 4.** На x4 сетка с 8 пробами содержит сетку с 4, точки-свидетели не добавлялись, оба решения удостоверены по непрерывному зазору до 10⁻⁴ мм. Пороги 0,02/0,002 мм в 200/20 раз больше. Поэтому правило ловит в основном смену точки KKT, а точность представления препятствий держится на юнит-тестах трубки. Усилить или заменить правило — решение владельца.

Это численная приёмка модели толстой нити на ветке. Ремесленная и художественная приёмка — за человеком; в `main` не принято.

## Второй ряд uwagake: описание до реализации (17.09)

**Ремесло.** По [Toolkit, uwagake](https://www.temarikai.com/HowToPages/ToolKit/uwagakechidori.html) (текст и фото 4–5):
- второй круг кладут параллельно снаружи первого, без обрыва — нить перенесена через начало ещё в первом круге;
- **верхний стежок** берут под всем пучком: разметка и прежние нити у острия; каждый круг примерно на толщину нити шире и ниже;
- **нижний стежок** — ниже прежнего острия, с припуском на поворот. Ориентир около 2 мм для перле №5 не постоянен ([практические замечания](https://www.temarikai.com/HowToPages/uwagakekiku.html)).

**Геометрия модели** (расчёт по плану C230, ветви — большие круги, r = 0,2 мм).

*Нижнее острие.* Угол между ветвями 13,6°, поэтому смещение острия вдоль луча на `a` отодвигает ветвь от прежнего прокола только на `a·tan 6,8° ≈ 0,12a`. Зазор от ветвей второго ряда до проколов первого при полуширине 0,6 мм:

| `a`, мм | 0,45 | 1 | 2 | 3 | 4 | 5 | 6 |
|---|---|---|---|---|---|---|---|
| зазор между осями, мм | 0,07 | 0,16 | 0,31 | 0,46 | 0,59 | 0,45 | 0,32 |

При необходимых 0,403 мм подходит `a` ≈ 2,6…5,4 мм. Это оценка только для ветвей к нижнему острию: замыкающая ветвь к открытому концу и окна в неё не входят. Сдвиг «на толщину нити», который годится для широкого верхнего острия (97°), внизу даёт проникновение.

*Пересечения рядов.* Ветви второго ряда неизбежно пересекают ветви первого: уход второго ряда пересекает приход первого примерно в `5 − a/2` мм выше прежнего острия (грубая оценка по прямым ветвям; в плане место отличается примерно на 1 мм, а у верхних острий и на стыке кругов пересечения лежат совсем близко к проколам). Это внутри нижнего окна (12 мм) — его решает контакт. Порядок задан хронологией: второй ряд поверх первого, пересечение объявляется.

*Верхнее острие.* Проколы на ±1,0 мм и на 0,45 мм ниже по лучу (уже заданы открытым концом круга). Скрытый подхват второго ряда идёт параллельно прежнему на 0,45 мм и проходит под перехлёстом первого ряда и разметкой — это «под всем пучком». Приход второго ряда кладётся поверх прежних нитей у острия (окно 8 мм).

**Решения до расчёта** (инженерные, пересматриваемые):
1. `a` = наименьшее значение из таблицы с зазором ≥ 2r + зазор, округлённое вверх до 0,5 мм: **3,0 мм**. Это не ремесленная мера: настоящая пряжа сжимается и может ложиться плотнее.
2. Полуширина нижнего стежка второго ряда — прежние 0,6 мм; верхнего — 1,0 мм.
3. Всё первого ряда (окна, подхваты, начало, дуги) — неподвижные трубки. Окна второго ряда решаются в порядке шитья тем же способом.
4. Объявляются: окна второго ряда поверх окон первого у того же острия; верхний подхват второго ряда под разметкой и под перехлёстом первого ряда; возврат второго круга поверх начала второго ряда. Необъявленные пересечения по-прежнему дают отказ.
5. Этап `row2` = первый круг + второй круг, открытый конец у входа третьего ряда. Приёмка — правила 1–5; с 17.09 правило 1 для второго круга действует в варианте 1 (см. «Приёмка»). Время расчёта примерно вдвое больше круга.

**Не решается этим шагом:** сжатие и уплотнение пучка, равновесие ряда с ранее уложенными (прежние ряды неподвижны), разные цвета наборов A/B GT14.

### Первый результат второго ряда: полное перестроение (17.09, `cc092e1`, до варианта 1)

Реализация — по описанию выше: стежки задаются парой «острие, ряд»; стежок и круг остались побитово прежними. Данные — `screenshots/s8-row2-2026-09-17/` основного чекаута.

- **Что работает.** На всех пяти уровнях и в контроле с частыми пробами:
  - все 32 окна сходятся и подтверждены перезапуском;
  - полная проверка нити `passed`, необъявленных пересечений нет;
  - доказанное `rκ ≤ 0,816`, скрытые ≤ 0,389; скрытые подхваты двух рядов у одного острия расходятся больше чем на 2r (тест).
- **Пересечения между рядами** находятся расчётом и объявляются по хронологии; порядок проверяет валидатор. У каждого острия — уход второго ряда поверх прихода первого и приход второго поверх ухода первого, как и предсказывал расчёт геометрии.
- **Вердикт: `unresolved`** — одинаково для острия 0 в обоих обходах и для острия 2 (около 31 мин на вариант).
  - Не проходит одно: у приходов и уходов второго ряда в верхних остриях (кроме стартового) разность **длины** на шаге x2→x3 — 6,15·10⁻³ и 2,42·10⁻³ мм при допуске 2·10⁻³.
  - На шаге x3→x4 — 1,1·10⁻⁴ и 4,1·10⁻⁴ мм; формы везде в допуске.
- **Причина — перенос предасимптотичности, а не сетка второго ряда.**
  - На замороженных данных x4 приход `approach-2-r2` при 54…253 пролётах отличается от самого мелкого не больше чем на 2,9·10⁻³ мм по форме и 5,4·10⁻⁴ по длине. Шаг 72→108 по длине — около 2·10⁻⁴ мм.
  - Скачок длины приходит с данными: на x2 уход первого ряда у верхнего острия стоит в другой точке KKT (1,3·10⁻² мм по форме, для первого ряда это в допуске). Второй ряд ложится поверх и превращает этот сдвиг в 6·10⁻³ мм длины.
  - Правило «каждый уровень — полное построение» (правило 1) переносит несходимость грубого уровня ранних рядов в поздние ряды.
- **Варианты (допуски не трогаем); владелец выбрал вариант 1 — он реализован, см. «Приёмка», правило 1:**
  1. **Ранние ряды — на самом мелком уровне.** Ранее уложенный материал берётся из самого мелкого принятого построения, а лестница уточняет только текущий ряд. Физически ранние ряды уже лежат, их точность контролирует их собственная приёмка. Это изменение правила 1.
  2. **Лестница без x1–x2 в сравнении.** Например, 3/3,75/4,7 (предел 256 точек у нижних окон второго ряда — 53 пролёта × 4,8). Это изменение правила приёмки.
  3. **Убрать неоднозначность x2 у верхнего ухода первого ряда** (две точки KKT) — отдельное исследование модели; причина не выяснена.
- **Диагностика варианта 1** (не приёмка; скрипт и данные — `r2opt1.mts`, `opt1.tgz` в той же папке).
  - **Как считалось.** Первый ряд взят с уровня x4 канонического расчёта. Окна второго ряда решены на 1/1,5/2/3/4 в порядке шитья, с остановкой и перезапусками канона. Полная проверка нити на каждом уровне в этом режиме не выполнялась.
  - **Результат.** Все 16 окон второго ряда сошлись и проходят асимптотическое правило при прежних допусках. Например, у `approach-2-r2`:
    - разности длины 4,5·10⁻⁴ → 3,1·10⁻⁴ → 1,9·10⁻⁴ → 4·10⁻⁶ мм;
    - разности формы 7,7·10⁻³ → 4,6·10⁻³ → 2,4·10⁻³ → 9,2·10⁻⁴ мм.
  - **Худший случай** — `departure-2-r2`: длина 4,0·10⁻³ → 2,2·10⁻³ → 3,8·10⁻⁴ → 2,0·10⁻⁴ мм. Грубые уровни предасимптотичны, последние два шага в допуске.

## Открытые вопросы

- **Модель у верхнего острия:** остановка, неподвижная точка и представление препятствий решены (17.09); стежок и круг приняты численно. Совместное равновесие прихода и ухода не исследовано: приход неподвижен, когда поверх ложится уход.
- **Нижний подхват C230** (`computed-lower-kagari.ts`) с 17.09 тоже считается трубками, со строгой остановкой и перезапусками.
- **Одна опора `curve` — одно ограничение на пробу.** Если нить ложится сразу на две ветви одной опоры (желоб), ограничение негладкое и решатель честно заканчивает `unresolved` (тест). В Simple 8 каждое окно, подхват и начало — отдельные опоры. У трубки вокруг туго изогнутой нити на вогнутой стороне тоже есть излом: расстояние контакта ≈ 2r больше `ρ_min` = 1,25r. Ослабление допуска и выбор удачной сетки не рассматриваются.
- **Физика:** трение, сжатие, реальное удержание проколов и калибровка `ρ_min` не моделируются.
- **Ряды:** второй круг построен (`row2`); сжатие пучка, третий и дальнейшие круги, цвета наборов A/B и C8 — впереди.
